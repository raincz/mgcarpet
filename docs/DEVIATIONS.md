# DEVIATIONS — deliberate departures from retail behavior

The canonical register of every place where mgcarpet INTENTIONALLY departs
from retail Magic Carpet 1/2 behavior: gameplay rulings, enhancement- and
preference-class options, deliberate approximations, and fixed retail bugs.
Extracted from inline code comments during the 2026-07-19 comment sweep; the
source sites keep a terse `(deliberate)` marker pointing here.

Maintenance law: when introducing a new deliberate deviation, add its ruling
here (what deviates from retail, and why) and mark the code site
`(deliberate)`. When an audit finds code diverging from the decompile, check
this register BEFORE "fixing" toward retail. Companion doc: docs/FIDELITY.md.

A few rulings surface in more than one layer (e.g. `prune_owned_jars` in the
sim, the config registry, and the app loader; `smooth_motion` in config and
settings; the MC1 duel-pull knock-channel transport in world.rs and
rivals.rs) — each layer's entry describes its own surface.

The mgc-sim test suite deliberately contributes no entries: tests pin
faithful retail behavior only; enhancement touchpoints are documented at
their source.


## Patch options (`gameplay · patches`) — retail bugs with both arms

PLAYER-MANDATED 2026-08-08: every deliberate upstream bugfix is an exposed
option — "things that retail messes up and we will do better", the
authenticity matrix extended to bug-for-bug fidelity. Each patch implements
BOTH arms (`retail` = the shipped bug, `patched` = the fix) as a Live,
hotkey-less toggle under the options menu's GAMEPLAY tab.

Fixture safety is STRUCTURAL, not advisory: `World::new*` defaults every
arm to RETAIL (`mgc_sim::WorldPatches::RETAIL`), so goldens, unit tests and
mgc-conform (which never reads app config) evolve under retail law unless a
caller opts in; `retail_import_mc1/mc2` re-force RETAIL as a belt and
`strict_retail` still overrides the gated sites; `--record` forces the
retail arms for the whole session (stamping `"patches": "retail"` into the
port header) and `--replay`/`--replay-check` pin to the take's recorded
policy — an older port header without the key replays under the pre-option
LEGACY set. Patches therefore never flip the fidelity rollup; the startup
banner reports their count apart.

The register (defaults player-ruled 2026-08-08 — `castle_recast_cost`
ships RETAIL, the rest PATCHED):

| option | default | the retail bug | entry |
|---|---|---|---|
| `castle_recast_cost` | retail | first-castle lockout (stale cost stamp) | World::spell_cast_cost below |
| `jar_ground_snap` | patched | jars buried/floating after terrain edits | World::class12_tick below |
| `ball_ground_track` | patched | settled balls hang mid-air / get buried | combat.rs::ball_tick below |
| `map_wide_ball_rolling` | patched | balls only roll near you ("mana runs away") | combat.rs::ball_tick / mob_awake_pass below |
| `possessed_footprint` | patched | possessed dwellings self-destruct | Gen::tick_building_live below |
| `mc2_downgrade_overflow` | patched | level-7 haircut overflows backwards | mc2/castle.rs below |
| `mc2_magic_mine` | patched | the mine trigger ships dead | mc2/effects.rs below |
| `mc2_dweller_invisibility` | patched | (fog compensation, not an upstream bug — see note) | mgc-render below |
| `win2_movie_score` | patched | levelw2 scored from the wrong bank | movie.rs below |
| `castle_latch_bug` | patched | maze castles from the wall corner (skippable hand-side scan) | combat.rs::proj_castle_ball_tick below |

Classification note on `mc2_dweller_invisibility`: the only patch in the
class that fixes OUR deviation rather than retail's — the dwellers' covert
mana-stealing design leaned on retail's very short draw distance (fog band
15..19 tiles, sprite cull at 20), and the port's extended fog exposed them
map-wide as safe long-range meteor targets. The patched arm gives them the
wraith's proximity concealment (below); the retail arm is the authentic
entity behavior (no fade — retail's concealment lived in the renderer, not
the entity). Presentation-only and hash-silent: the sim never reads the
arm, so unlike its sim-affecting siblings it needs no fixture forcing —
`--record`'s blanket retail forcing covers it anyway via the config path.

CENSUS of the remaining deliberate-fix candidates (categorized 2026-08-08;
"every fix ultimately an option" applies only where a retail arm is
IMPLEMENTABLE):

- **Deferred, player pick needed** — MC2 stagevar death-watch (the
  authored-law port vs the autosave-severed shipped coin: the retail arm is
  a PER-MACHINE constant, there are two shipped behaviors to choose from);
  the kind-4 join arm (its clean port was removed per the player-certified
  retail observable; re-adding it is a feature, not a gate flip).
- **Ineligible — no faithful retail arm exists**: death_regrant retries
  (a one-shot loop in OUR allocator would eat spells far MORE often than
  retail, whose rebuild+sacrifice machinery is unported); player_land grave
  retry (retail's arm is a shipped memory-corruption bug — never porting
  it); every APPROX-class entry in this register (ball
  merge ring order, duel transports, doomsday scans…) — an approximation
  has no second arm to offer.
- **Not patches** — port-bug fixes toward retail (rival cast gate, mana-hunt
  offense gate, ambient-fire extents, balloon tether…) are faithfulness
  work, not deviations; preference/enhancement-class deviations
  (prune_owned_jars, kill-matrix compaction, map/AA/audio polish) stay in
  their own classes; the MC2 native reap timing and recycle refresh cadence
  are unfinished-port approximations tracked in their own entries.

## mgc-sim — shared world core (mc1/world.rs)

- **world.rs module header** — Faithful subset only: no AI wizard balloons (the probe/scan lists are the player alone); custom family behaviors beyond movement/combat (disguises, mana hunts, house building, teleports) stand still pending the AI track; class-12 pickup/mana transfer not ported (mana balls drop, merge and take claims but nothing collects them); sounds omitted. Tracked in docs/ROADMAP.md.
- **World::prune_owned_jars** — Unfaithful improvement (P-class option, default ON): removes any spell jar the local player already owns. Retail does NOT leave owned-spell jars visible: MC1's wizard init claims one class-12 entity per carried spell as the live manifestation and HIDES it (byte[0] |= 1, :54907) — the pool entity stays, invisible (slot economy). The mc1l1 corpus pins it (t=0: owned-spell rows flags 5, the collectable Accelerate jar flags 4). Strict-retail worlds honor the bit (the class-12 skip in `live_things`/`live_poses` keys on `flags & 1`, replacing the old phase-0 heuristic that still drew a wizard-CLAIMED authored jar) so imported/replayed retail states look right; the free-running port models owned spells OUTSIDE the pool and never mints manifestations, so this option is its free-run stand-in — a REMOVAL where retail hides (the slot-economy difference is the residual deviation). Full manifestation-entity fidelity is an open lead (it matters once rival-wizard spell inventories land).
- **World::class12_tick (jar ground-snap)** — PATCH OPTION `gameplay.patches.jar_ground_snap` (default patched; was always-on until 2026-08-08): a resting jar re-snaps to its tile's ground every tick. Retail's terrain-reshape walk re-snaps class 2 and kills class 5 but default-skips class 12 (:51729), so jars end up buried and unpickable under later level shaping (HW authors several such — a release-QA failure) or float in the air after earth-shaping spells (earthquake). Ruled critical to correct; the snap keeps every jar collectable and grounded. Conformance replay is exempt: `retail_import_mc1` sets `World::strict_retail`, which disables deliberate gameplay deviations so an imported retail state evolves under retail's frozen-z law exactly (without the exemption the snap manufactures systematic z offsets against retail recordings — docs/CONFORMANCE-FINDINGS.md, resolved entry).
- **World::tick (MC2 sweep: disabled dispatch)** — Native MC2 play runs a disabled entity's handler once more when the sweep reaches it, then hard-frees (record zeroed). Retail (decompile-corroborated 2026-08-01, `UpdateEntities_57730` EF:39948-56 → `sub_57F20`) instead reaps every disabled record in one unconditional pass at the TOP of the next tick — the record survives exactly one end-of-tick snapshot, never runs again, and its slot returns before that tick's spawns pop the stack. Strict-retail replay now runs that exact tick-top reap (the MC2 arm of the shared MC1 reap pass, world.rs `tick()`); native MC2 keeps the in-loop run-then-free until the native sweep-law port settles its timing against the dome/eruption chain (mc2_slice guards it). RESOLVED FOR NEWBORNS 2026-07-30: the earlier "newborns never tick in their birth pass" reading was the t=0 special case — retail's frame pass (EF:40116) is a bare ascending pointer walk, so a mid-pass spawn ticks the same pass iff its slot lies AHEAD of the cursor (the mc2l0 chimney corpus pins it: no life-32 smoke record exists, every newborn surfaces one step in). The port's natural ascending loop reproduces this in BOTH native and strict-retail play, so the universal newborn gate is gone and the dome/eruption chain's same-pass summit spawn is faithful, not a deviation (mc2_slice's dome test still guards it).
- **World::won** — On win, the app fades out and ends the game with no stats screen / campaign-stitching sequence (deliberate simplification; "yet" — still open work).
- **World::tick (MC1 duel pull)** — The caster's duel pull magnitude formula is traced from retail (:55228-48), but it is applied through our own knock channel rather than retail's transport (deliberate approximation).
- **World::player_land (jar scatter)** — RETIRED AS A DEVIATION (MC1). The entry read: "uses the same LCG constants as retail (:55519-47) but draws from the world RNG stream rather than the dying wizard's private stream, which is not modeled outside flight". It sat on GRADED lanes — every scattered jar's x/y/act_life plus the pair's rng draw count — so it floored the certified run, and the standing player rule retires such a ruling on sight. The private stream is `*(a1+4)`, the carpet ENTITY's own `rand` (:55538-46), and a conformance import DOES have a home for it: the human slot's record is zeroed but its `rand` is now seeded from the recording, and `player_land` draws from `mc1_carpet_slot`'s `rand` whenever the pool carries the carpet (native play, which has no pooled carpet, still falls back to the world stream — that residue is the MC2 twin's shape, below). The scatter also walks retail's ACQUISITION list (`+532`, :55519-24) rather than the spell table, which is the other half of the same observable: the order picks which jar gets which triple. mc1l42 t=17343 closes exactly: five jars, jitter and ttl bit-exact, and the pair's world-draw count back to retail's 1.
- **combat.rs::ball_tick (settled balls track the ground)** — PATCH OPTION `gameplay.patches.ball_ground_track` (default patched; was always-on until 2026-08-08). PLAYER-RULED 2026-07-30. Retail freezes a mana ball wherever it is when its 128-tick settle budget (+58) expires: a ball still mid-hop hangs in the air forever (worm deaths down a slope leave floating balls — verified faithful, the whole physics body is behind the `else if (+58)` gate and nothing ever re-snaps an expired ball), and a later terrain edit (volcano, castle stamp, crater) BURIES a grounded one, since only live-physics balls get the ground clamp. The port's settled arm snaps `z = ground` every tick, both directions — hovering balls land, buried balls surface. MC1-scoped by construction (MC2 spheres never settle) and OFF under strict-retail replay. Pinned by `a_settled_ball_tracks_the_ground_instead_of_freezing`; L005 GOLDEN B-E re-pinned (OBSERVABLE holds — the window's snapped deltas sit under the pose projection).
- **World::player_land (grave)** — On a full entity pool, retail retries the whole death landing next tick; ours proceeds graveless (loose mana balls stay player-owned) — a benign deliberate deviation. Deliberately NOT ported with the retry: on retail's second landing pass the 24 spell slots already hold banked MODEL ids, which the jar loop reinterprets as ENTITY indices — model 0 destroys that spell outright and models 1..23 hijack pool entities 1..23 (owned-bit cleared, state bumped, teleported to the corpse with a 200-289 life) — a shipped corruption bug riding the retry (:55550-66 — state 3 is only set inside `if (grave)`, so state 2 re-enters whole).
- **World::death_regrant (respawn re-grant retries; retail eats the spell)** — Player-favoring deviation 2026-08-05 (the intermittent "death eats my hand spells" report, mc1:49 + mc1hw:0). Retail's respawn re-instantiation loop (:54884-923; HW :50952-991, byte-identical) is ONE-SHOT: the 24-slot array `+532` is a phase-tagged union (alive = manifestation entity index, dead = banked model id), and the alloc-failure branch overwrites the banked model id with 0 (:54917 / HW :50985) — the spell is permanently, silently lost (no jar: the scattered jars decay on their own 200-289 fuse regardless). Retail makes that failure RARE: `sub_44D30` first rebuilds both free lists by scanning all 1000 entities (:54842 → :43825-61), and `NewEvent_372C0` falls back to sacrificing recycle-victims (:43910) before returning null — the respawn window is the one place MC1's recycle stack is populated (emptied again at :55056). The port's MC1 allocator has neither the rebuild nor the sacrifice, so a faithful one-shot loop would eat spells far MORE often than retail (the reported bug). Rather than port the sacrifice machinery, the port keeps the `death_owned` bank entry on a starved grant and retries from the wizard tick every tick while alive — the spell returns as soon as any slot frees (the scattered jars' own decay guarantees pressure relief within ~289 ticks). Hands are untouched either way (retail never writes +940/+944 on death or respawn — verified, writer set is level-init + input handler only). Pinned by `respawn_on_an_exhausted_pool_never_eats_the_inventory`.
- **combat.rs spawn arms (universal f34/f36 target mirror)** — RETIRED 2026-08-13 (was PLAYER-RULED 2026-08-01). The mirror is gone site by site: `arm_projectile` lost it with the thunk-bolt muzzle law (pinned by `a_thunk_bolt_aims_from_the_unlifted_muzzle_and_is_born_with_zero_target_yaw`), and the last survivor — `corpse_drop_mc1`'s ball drop — now writes only +30, matching retail sub_27690 verbatim (pinned in `fireball_kills_and_the_corpse_drops_a_mana_ball`; the ball tick never reads f34, so the fix is motion-inert). The 2026-08-01 rationale ("splitting the shared arm risks the homing paths") stopped applying once the arm sites stopped sharing the write. The cast emissions (player + rival class-9) KEEP the mirror — retail's own emission writes the aim there (corpus: live (9,x) records carry nonzero target_yaw from birth). The three `mc1-spawn-arm-f34-*` roster rules are deleted: the ball rule's whole 129-row mc1l0 family died with the fix, and the fire/flame rules' remaining hits were misattributed slot-collision rows (class 10-vs-9 pool mismatches wearing the fresh-fire slot), which now surface honestly. Ledger: CONFORMANCE-FINDINGS.md "THE CORPSE-DROP f34 MIRROR FALLS".
- **World::cast_projectile (launch pitches)** — RETIRED AS A DEVIATION 2026-08-17 (was: "down-arc terrain spells get a fixed downward pitch bias rather than retail's per-spell launch-pitch table"). There is no such table: meteor (:65402-04), volcano (:65477-80) and crater (:65533-34) all copy the caster's `+32` VERBATIM, so the down-arc is the player's own aim and the approximation was putting every lob 96 units of pitch below retail (mc1l42 t=20150: retail 136, port 232). Pinned by `the-crater-launches-on-the-caster-s-own-raw-aim`.
- **World::mc2_teleport_random** — The no-castle random teleport hop reuses `ent_rand` for determinism instead of retail's `9377·r+9439` LCG stream (deliberate; exact stream banked).
- **World::mc2_duel_tether_tick** — The tether grip range uses the tier's enforcement range because retail's own grip-write instruction is not isolable in the decompile (deliberate approximation; a farther grip would dissolve on the next enforcement pass anyway).
- **World::thrust_cancel (MC2 Speed)** — A braking (reverse) thrust interrupts the MC2 Speed armed window early and clears its burst timer. The literal decompile (`GetScroll_69DB0`) hard-overrides speed every tick with no brake input; interruptibility is restored from recorded gameplay over the trace (deliberate — recorded gameplay ranks above the decompile).
- **World::mc2_objective_targets** — The objective-guide overlay's "nearest target" arrow uses a torus-wrapped (shortest-way-round) distance metric, deliberately unlike the type-5 fly-to LATCH which uses retail's plain sign-extended abs. The overlay is a UI heuristic; the completion latch stays faithful.
- **World::spawn_from_thing (10,22 whirlwind)** — Disposition-spawned arm tornadoes are scaled to their tier under the same 8×charge law as the cast path, even though remc2's generate switch omits model 22 (which would leave a 500-tick roamer). Unified over the trace based on recorded retail (deliberate).
- **World::mc2_end_tick** — The MC2 ending sequence deliberately skips presentation elements: the retail moveTest terrain-block abort (the glue keeps the carpet above ground), the fov dolly-zoom (phase 5), and launch motion blur. The roll/pitch auto-level tail is handled app-side.
- **World::max_ground_tiles** — The global lift ceiling (level's highest terrain tile + the soft-ceiling band) so explicit lift can never reach a god's-eye view (deliberate gameplay ruling). Since the 2026-07-22 desired-altitude law the everyday cap is GROUND-RELATIVE (the per-game climb band); this absolute ceiling backs the `dev.lift_unclamped` instrument and the enhanced mover's rising-cap safety.


## mgc-sim — shared engine state (mc1/features.rs group)

- **features.rs::Gen::tick_building_live** — RETIRED 2026-07-23 (was: "retail grants the claimer an immediate mana credit off the claimer's +48; omitted, deferred to the mana-economy track"). The reading was wrong: retail's `+86 += *(claimant_wizext + 48)` (:30808-09) adds the owner's COLOR onto the sprite-type field +86 — it is the claimed-flag recolor (row 177 + team, the same wizext var_48 family mechanism as the claimed-ball rows), not a mana movement. Now ported; there is no mana credit in the ch1 claim block.
- **features.rs::Gen::tick_building_live (possession preserves the footprint extent)** — PATCH OPTION `gameplay.patches.possessed_footprint` (default patched; was always-on until 2026-08-08). The possession re-owner stamps the owner-flag sprite (177). Retail's `sub_36FA0_37360(_, 177)` (:30808) overwrites the building's +78/80/82/84 with the tiny flag sprite's extent — but the villager-emit (`sub_28D10`, x + f80) and defender pop-out (x + f80) spawn against that same +80. With the footprint extent clobbered, those spawns collapse from just OUTSIDE the footprint to ON the roof, where the creature is walled-in, dies, and its 400 corpse-flame damages the very house you possessed — a self-sustaining collapse that destroys a possessed village dwelling within a few thousand ticks (reproduced deterministically on the mc1:0 beach house). The port keeps the flag sprite but PRESERVES the footprint extents, so emits/defenders keep spawning on clear ground. Pinned by `possession_preserves_the_dwelling_footprint`.
- **features.rs::Gen::castle_balloons (retarget stagger)** — RETIRED AS A DEVIATION 2026-08-12 (was: "balloon retargeting is re-picked every dispatch pass; retail staggers by `castle+63 % fleet`, an untraced nicety"). The mc1l0 (3,3) block (163 rows) indicted the approximation and the trace closed it: retargeting runs ONLY on passes where `castle+63 % quota == 0` (:56338) and a freshly spawned fleet index gets NO targeting that pass at all (:56340-49 — the newborn parks at the flag with chase 0, mc1l0 t=2379); between turns every balloon keeps its stale `+146`, even one pointing at a slot that has since been FREED — and the mover (sub_47F90) is BLIND: it routes on the target's class byte alone, no liveness or model check, so a balloon whose ball died mid-flight bounces forever across the corpse's stale position (t=2472-2516, the ±48 y-bounce, angle(0,±48) flipping 0/1024). The census-full arm bypasses the stagger (:56333-35); the ball pick metric is 3-D (sub_42390 includes z); a dead register balloon reaps at dispatch time with the cargo drop (:56345-47). The old port-side dead-target guard and the model-39 stale-slot guard idled/cleared where retail chases — both removed (the recycled-record absorb, :56742-73, is retail's own latent bug and now the port's law). Pinned by `the_balloon_mover_is_blind` + `the_dispatcher_staggers_retargeting_and_a_fresh_balloon_parks_untargeted`. REMAINING (kept, still deviations): the 3-slot fleet REGISTER in the wizard side table (+52) is not modeled — fleet membership/cull order comes from an owner scan in slot order (same membership, the culled EXCESS may differ); the census's house tally is a live (10,45) scan rather than retail's accumulated `+308`. **The ball-pick clause is RETIRED 2026-08-16** — it read "the ball-pick list is the live state rather than retail's tick-top rebuild (a ball flagged EARLIER in the same tick stays visible to retail's scan, invisible to ours — no corpus row demands it)", and mc1l2 supplied the demanding rows: 45 of them (38 (3,3) + 7 (10,39)) at t=699/857/1349/2409/2435/5443/5767/7519/9549. The pick now walks `Gen::ball_chain` — retail's `sub_46CA0` (:55931-43) is a LINKED-LIST walk from `var_u32_36462[1]`, the head the tick-top sweep rebuilds at :52287-300, and its filter is `model65 == 39 && +144 == owner` with NO class byte, NO act_life and NO 0x400 test. `sub_41E80` (:52508-11) is only `flags |= 0x400`; the reclaim `sub_41E90` (:52514-20) runs from the per-slot walk at the NEXT tick's top, so a ball the balloon drank earlier in the tick is still a member and the castle-slot dispatcher re-locks onto the corpse it just absorbed. Dropping the class test is decompile-mandated as well: the mid-tick merge reclaim (`sub_277D0` :29723+) clears `class64` but leaves `model65`/`+144`. HW twin :51986-52008 identical. ⚠ STILL OPEN, same function family: `Gen::castle_absorb` (features.rs) carries the IDENTICAL divergence — a live-pool scan plus a `flags & 0x400 == 0` gate — against `sub_46CA0`'s sibling at :56024-34, which walks the same chain with the same two-test filter. Retail would RE-ABSORB a ball flagged earlier in the tick, double-counting its mana. No corpus row demands it yet and it is the riskier of the two, so it was deliberately left out.
- **features.rs::Gen::castle_downgrade (total-death scatter + fleet demolition)** — RETIRED AS A DEVIATION 2026-08-12 (were: PATCH OPTIONS `castle_death_mana` / `castle_death_balloons`, default patched, premised on retail's `!level` arm (sub_47A70 :56531-37) never running the ejector and never touching the fleet). The premise missed the state-6 WRAPPER: `sub_470E0` (:56147-50) calls `sub_47130` and `sub_47400` AFTER the teardown returns, so retail itself scatters the whole bank through the ejector's level-0 all-stored arm (:56189-90) and demolishes the fleet through the level-0 quota cull with the cargo spill (:56399-411). REFUTED BY CORPUS mc1l0: t=2217 castle slot 107 dies holding 8302 → retail leaves residual 6 = 8302 % 8 after 8 balls of 1037 (the ejector's own count/share arithmetic), spawns the 4 ejector magnets, soft-kills the balloon with the cargo drop (flags 1036), and re-caps the dead flag's +136 to CAP[0] = 5000 (sub_47BD0 row 0: cap unconditional, HP row-gated at 0 — no life re-stamp); t=1363 = the same law at 3000 → 3×1000 residual 0. Both behaviors are now unconditional retail law in `castle_downgrade`; the DEVIATIONS entries anticipating "a retail replay showing the residual scatter" are hereby collapsed by exactly that evidence. Pinned by `castle_total_death_demolishes_balloons_spilling_cargo` + the castle-death scatter test.
- **World::spell_cast_cost (the castle-cost stale stamp / first-castle lockout)** — PATCH OPTION `gameplay.patches.castle_recast_cost`, **DEFAULT RETAIL** (player-ruled 2026-08-08 — the lockout may be a deliberate period challenge; the fix is the opt-in). Retail prices Create Castle through a LIVE CACHE: the spell-16 manifestation's `+136` is rewritten to `CASTLE_CAP[castle level]` at every castle init/level-up (sub_47C60/sub_47DD0 — the next cast while housed IS the upgrade, and the HUD dots divide the pool by this live word, sub_23D40 :27703), and **the teardown re-stamps it at `CAP[0] = 5000` on total castle death** (sub_47A70 :56527-28 runs the ladder stamp AFTER the final level decrement — CORRECTED 2026-08-10; this entry previously claimed no teardown re-stamp and a stale 10000. Proof: the cast-phase corpus's castle-token `+140 = 49` = 5000/101, and the mc1l5 take's Vodor rebuild fires exactly when his census ceiling crosses 5000). A homeless wizard therefore prices the re-cast at 5000 whatever level the dead castle held, producing the retail FIRST-CASTLE LOCKOUT: starting mana (1000, = the ctor cost exactly) affords the first cast, but after that castle dies the re-cast is unaffordable until collected claims push the census ceiling past the death stamp. PLAYER-CERTIFIED ON RETAIL 2026-07-31: present in all three games, identical across every castle death mode, first-cycle-per-level only (later cycles keep ≥CAP worth of claims in the census via the castle bank / ejected balls — but a rival stealing the spill re-arms it: the mc1hw:0 Vodor case). NOT rival-immune (CORRECTED 2026-08-10): the rival build want/commit gates read the SAME live stamp (sub_15E90 :19375 / sub_15A00 case 0x10 :19332 — `rivals.rs::rival_castle_price`), which IS the rival castle-rebuild POVERTY GATE; the every-tick standing re-stamp alone is human-only (mc1l5 t=0: Vodor's token holds the ctor 1000/9 under his standing authored castle — the rival init order leaves wizext+708 empty when the authored-castle init stamp runs). BOTH ARMS IMPLEMENTED 2026-08-08: the manifestation cache is now faithful STATE under either arm (`grant_spell` seeds the ctor +136=1000/+140=9, the castle dispatch re-stamps every tick a castle stands — native worlds bind exactly through `player.owned[16]`, imports keep the f144 join), and only the READ forks — retail arm: `spell_cast_cost` returns the cached +136 (the lockout; the HUD dots reproduce retail's stale display); patched arm: live-law re-derive (housed → `CAP[level]`, homeless → ctor 1000, the 2026-07-17 fix). MC2's face is the lock-release re-sync scope in mc2/cast.rs: while a castle stands the `sub_60780` re-sync runs under both arms (the downgrade-ding fix is faithful), but the castle-less release re-syncs only under the patched arm — retail's stamp rides castle stat writes that no longer exist. Pinned by `first_castle_lockout_stale_stamp_vs_live_law` (MC1, both arms) and `mc2_castle_death_{keeps_the_stale_cost_on_the_retail,resyncs_to_base_cost_on_the_patched}_arm`. Conformance now runs the retail arm — a cast-after-castle-loss take conforms instead of flagging here.
- **combat.rs::proj_castle_ball_tick + World::cast_castle (the castle "latch bug" — maze castles)** — PATCH OPTION `gameplay.patches.castle_latch_bug` (default patched; player-ruled 2026-08-08). MC1/HW only; the MC2 lane keeps its pre-option behavior under BOTH arms (EF lineage unverified for this structure). Certified against `recordings/mc1l32-castle-bug.mgcr` (level 032, the no-castle portal maze): retail raised a castle ON a protected maze wall at tile (16,234) and its painter carved the "impenetrable" walls, from a cast the port refused. THE RETAIL LAW (three cooperating defects): (1) the castle ball spawns at the HAND muzzle — `sub_55EF0` :56420, ±256 units at yaw∓512 per the firing hand, reverted to the carpet when the muzzle pokes below terrain — and the launch-tick placement scan `sub_12F70` :17786 samples THAT tile, so the aim steers the scan anchor around a one-tile circle (the recorded 60-unit yaw change moved the muzzle 3 units across a tile boundary and flipped fizzle→build); (2) the scan's 8×8 protection window is anchored strictly NW — tiles (tx−8..tx−1, ty−8..ty−1), never the anchor tile itself nor anything S/E — so a cast from the SE side of protected geometry samples only open corridor; (3) the landing condition short-circuits (`ground > z || --life < 0 || !scan`, :63586-90): a terrain TOUCHDOWN builds the castle at the contact point with NO placement check (the wall face directly ahead grounds the ball on its first flight tick); the scan re-runs only on airborne ticks, where a failure stops the ball early — flip 180°, one step back with the live pitch (:63601-04) — and STILL builds. Once launched, a castle always rises; the launch scan is the only real gate, and the level's defense is protection-bit skirts on the floor tiles flanking every wall (the wall tiles themselves carry no bit). The RETAIL arm implements all three faithfully; the PATCHED arm (native default) keeps the pre-option port behavior — ball anchored at the carpet (the scan samples where you are; un-steerable) and the landing always re-scans, displacing a refused site one step back. KNOWN PATCHED-ARM RESIDUAL (documented, accepted): the landing re-scan cannot REFUSE — parked inside a clean-window tile (l32's (14,233)) the patched arm still builds, displaced into the corridor; the protected wall itself survives. Shared faithful details landed with the arms (both arms, MC1/HW): the launch tick latches+scans WITHOUT moving (:63612-21 — the recorded ball sits unmoved at its first boundary), the carpet-speed launch boost decays 2/tick toward the ctor 384 (+128 is never bumped; recorded 464→462), the moved ball keeps its stepped z (no ground clamp; recorded z 7344 under ground 7808), the life countdown runs on airborne ticks only, and `castle_site_ok`'s proximity term is `abs16 <= castle_extents + 2048` (the probe's extents play no part). Pinned by `spell_castle_latch.rs` (4 arms/spots, incl. both recorded casts tick-exact). The verify-deltas run on the take: 601/601 pairs fixture-grade, rng clean, the build lands at the recorded tick+tile; residual lanes = castle z +64, painter (10,42) ctor fields (life/chase), the ±1-tick raw-input aim caveat on the two cast-reconstruction pairs (the reconstructed aim moves the muzzle 1 unit across the tile edge — the cheese is that tight), plus the take's pre-existing l32 lanes (balloon z-resnap, dolmen mana_max, class-12 ecology churn). PLAYER-CERTIFIED 2026-08-08 (retail-arm playtest). Player lore, same certification: this is one face of MC1's known PROTECTION-DESTRUCTION bug family — cast from unprotected ground AIMED INTO a protected area and the ball either touches down inside it (build unscanned) or trips the airborne scan at its edge (flip + step back + build anyway): either way a castle rises against the protected geometry and the build/destroy cycle strips the protection bits piece by piece, wall by wall — with enough patience an entire maze's protection can be erased from outside. The inside-out variant on the take is the rare cousin; the outside-in chisel is the common form, and the retail arm reproduces both through the same three defects with no extra code.
- **features.rs::Gen::tick** — During the load-time fixpoint pass `ctx` is `None`, so the terrain deformers (hill/digger/etc.) skip the ch0 damage broadcast and the loop-10 rumble. Retail's load pass DOES broadcast into the half-built pool, but nothing observable survives it, so the port suppresses it and only broadcasts once the world is running (ctx = Some).
- **features.rs::Gen::mc2_rebuild_recycle (native victim-list REFRESH CADENCE)** — The victim ranking itself is retail's, verbatim: `sub_49F90` (Level.cpp:1284-1301) rebuilds the recycle stack by a descending 999→1 scan of live records carrying `byte[2] & 2`, so `NewEvent_4A050`'s full-pool arm (Events.cpp:581) always sacrifices the LOWEST-numbered sacrificable entity. What the port does not model is WHEN that list is rebuilt: retail refreshes it at `sub_49F90`'s own call sites (fresh-level generate EF:39396, the class-3 arm EF:60101, the mana-overflow path EF:61275-79 — the last is literally "free stack empty ⇒ rebuild ⇒ retry"), and empties it outright at every save/load (`sub_49F90(); dword_0x11e6 = -1;`, Level.cpp:304-305/:423-424, EF:38829/:38874/:39467). Native MC2 play instead re-ranks the list on demand, inside the allocator, the first time a spawn finds the free stack dry — so a full pool that retail would have let starve (stale, drained list) can still sacrifice here. The window is narrow by construction: the rebuild can only run with the pool FULL, where retail's own scan would find no free record either, and each seizure wipes the victim's sacrificable bit so the list strictly shrinks. STRICT-RETAIL REPLAY IS EXEMPT — `retail_import_mc2` installs the recorded stack with `refill` clear, so replay sacrifices exactly retail's ranked victims and starves exactly where retail's list ran out. Pinned by `mc2_full_pool_sacrifices_the_lowest_ranked_victim_natively` (native) and `mc2_full_pool_sacrifices_the_recorded_recycle_victims_in_order` (strict). Ledger: CONFORMANCE-FINDINGS.md Resolved "THE MC2 ALLOCATOR'S FULL-POOL ARM".


## mgc-sim — MC1 combat + rivals

## combat.rs

- **combat.rs module header (`sub_12B50` mail write)** — The decompile's inverted accumulate/overwrite in `sub_12B50` is NOT ported; the direct write uses the area writers' protocol (:17301-05). Deliberate: the transcription swap is a suspect maintainer artifact, like :21814.
- **combat.rs module header (m9 ranged thunk aim)** — The m9 ranged thunk aims at the TARGET, not the decompile's `atan2(0,0)` self-aim (:21947-48). Deliberate: the self-aim is a decompile casualty.
- **combat.rs module header (aim assist metric)** — RETIRED 2026-08-12 (was: "aim assist scores candidates by angular miss Δyaw² + Δpitch², deliberate approximation of `sub_54A90`'s metric; exact port open for the CREATURE cones"). Every acquire subtype now runs the exact retail scan (`Gen::acquire_score` + `aim_assist_mc1_cone2`): sub_54A90's distance-weighted score (the 2-D ground distance decomposed onto the angular-error axes — 16.16 cos terms >>16, sin terms >>14 through an i16 truncation — so the CLOSER candidate wins unless the farther is far straighter), the 2-D 5120 range (sub_423D0 has no z term; the old port gated 3-D), the class-3 significant list pre-gated at the OWNER row's v_28 in 3-D at RAW positions (:64018-19; the beam's case 9 gates on its own `f128 × max_life`, :64137), the FULL class-3 candidate set (no model filter — castles at the RAW flag z via the sub_54BD0 twin, mana BALLOONS lifted +78; the "wizard-only" blocks 7/8/B/C carry no model filter either, :64100-118), and retail's scan order (significant list first, then the 20 creature buckets model-major). Pinned by `the_acquire_score_prefers_the_closer_candidate` and `balloons_are_acquire_candidates_and_v28_pre_gates_the_list`. Ledger §THE ACQUIRE SCAN EXACT (2026-08-12).
- **combat.rs module header (m9 lightning explosion)** — RETIRED AS A DEVIATION 2026-08-17 (was: "at the explosion, the +146 field stamps hit-or-0 where the original writes garbage on a miss; deliberate, remc2's `sub_66750` guards this"). It is not garbage. `v19[73] = (v17 - v21) / 164` (:63428) is an UNGUARDED pointer difference — the very next statement guards `v17` for the shielded-wizard quartering (:63437-47), so the author knew it could be null and left this one bare — and a null probe therefore records `(0 - entBase) / 164` truncated to a word, a CARPET.EXE link-time constant identical in every retail instance. Measured off the recording at **64608** (all 542 mc1l42 (10,23) miss rows and its 13 (10,11) crater rows) and now emitted as `mc1::mobs::MC1_MISS_STAMP`. **Player-ruled: a deviation on a GRADED lane puts a permanent floor under the certified run, which is the one thing a registered deviation may not do.** mc1l42 field rows 2,033 → 1,510, and it unblocked the kraken burst fixture (all 143 cadence ticks had been dirty by construction, and `PairDiff::clean()` deliberately ignores the roster so no suppression could have). ⚠ PER BINARY: HIDDEN.EXE links its pool elsewhere and its constant has no corpus witness, so the HW column still stamps NewEvent's 0 — measure it from an hw take before cutting it in. ✅ the (10,38) storm-cloud site (`combat.rs` `proj_m12_tick`) HAS its citation and now stamps too: `*(_WORD *)(v19 + 146) = (int)(v6 - base) / 164` (:63778) is the same unguarded pointer difference in the same shape, `v6` being the storm carrier's own probe result, and mc1l42's storm clouds read 64608 on every miss (t=27217 slot 99). Nothing about that write was ever storm-specific — every `+146` stamp in the engine is this idiom.
- **MC1 boulder throw filter (state 15)** — RE-BOUND to the generic flight 2026-08-08 per CARPET.EXE's relocated class-9 table (state 0xF → the bare `sub_52770` thunk 0x53060; remc1's transcribed table `str_25573C` :4838 is truncated at 0x0D — the only short table in the block; the orphan `sub_542B0` belongs to state 0x12). The former "straight flight" reconstruction rested on a misreading: the throw ctor `sub_1AE30` DOES pre-target the bolt — :22122-23 copy the thrower's own `+146` onto it and bind row [6] — so a thrown boulder PITCH-homes at its victim while never turning (row 6's yaw cap v_2 = 0, :5247). The bare thunk drops no fire trail (that wrapper is state 3's own `sub_53070` :63021), m14 is the acquire switch's default-refuse (`sub_54520` :64185), and the impact still carries the thrown `+44 = 780` through the (10,0) (:22112, sound 3 :28114) — the arrow quartet stays state 13's alone. THE REMAINING (deliberate) deviation: `arm_projectile` passes the shared `(3, 0xFF)` victim-filter pair where retail copies the thrower's own `+66`/`+67` (:22125-27) — adopt only after the victim_scan filter law is read. Pinned by `troll_boulder_rides_the_generic_flight_pretargeted` (pitch-homing + refuse + no-trail) and `troll_boulder_is_silent_in_flight_and_booms_on_impact`.
- **Gen::proj_bolt_tick (arrow reuse is FAITHFUL)** — Recorded here because it reads like a bug and must not be "fixed": retail plays the `arrow1`..`arrow4` samples for EVERY user of class-9 state 13 — the archer-type creatures m4/m9/m10 and the m15 castle guard — including m9, whose projectile wears a different billboard (sprite 203, :21957 — row 203 is sprite family base 215 against 195's base 193, same 45x60 size and 5-view fold, so it is art-only). `:63799` is the binary's sole emitter of those ids. Genuine period asset reuse; leave it.
- **Gen::spawn_bomb_fuse / Gen::bomb_fuse_tick** — Global Death's m18 fuse (state 19) sits past remc1's transcribed class-9 table. Reconstructed from observed retail behavior (never a bolt: fire once, wait, blast lands around the caster) as a caster-anchored fuse — 21 ticks tracking the caster, then a generic +44-copying detonation into the (10,55) field at the caster's position. Deliberate reconstruction; the ctor's speed/aim/+150 target and the +26 charge byte stay unmodeled. OPEN: retail may allow multiple overlapping charges each on its own delay.
- **Gen::ball_merge_candidates (within-ring visit order)** — The mana-merge partner search is retail's map-tile ring walk (`sub_10A50`/`sub_11D10`: rounded base tile, `(applied_pitch+255)>>8` rings, outward), but the visit order WITHIN one ring comes from a data table (`bitmaps_E9980x`) the decompile does not carry; raster order stands in. Deliberate approximation: it only decides which of two SIMULTANEOUSLY overlapping partners is absorbed first (measured cost: 12 (10,39) missing rows on the mc2l24 full take). Ledger: "the fountain over-spawn was retail deaths" Resolved entry.

## rivals.rs

- **rivals.rs module header (hate feed timing)** — The AI hate ledger is fed at damage-intake and homing-acquisition time instead of the original's per-projectile one-shot ledger scan (`sub_16540`). Deliberate: equivalent inputs, slightly later timing.
- **mobs.rs creature scans (creature targeting — WIDENED 2026-07-28)** — Wild creatures now acquire the full class-3 wizard list (`Gen::nearest_wizard_target`), matching retail's bucket[0] scan (`sub_19D70` :21519, rebuilt at :52253): the wyvern/crab/mound/guard scans take carpets, castles AND mana balloons (no `+65` filter — castle/balloon membership + the m9 castle-footprint reach at :24201 confirm castle-attacking is live retail behavior); the genie's aggro + mana-hunt scans take wizard carpets only (retail's `+65<=1` body gate). The m4 militia (:22613) and m8 griffon (:23500) wanted-gates are now per-wizard: `Gen::rival_wanted[8]` mirrors the human's `player_aggro` for the rivals (the +528 timer), armed by a rival's own village offenses through `Gen::flag_village_wanted` at the four writer sites and decremented alongside `player_aggro` — so a village turns its militia (and the griffon flock) on any wizard that harms it, not only the human. Two small residuals remain: (1) the shared wander/crab scans apply the port's human-owner (undead-army) exclusion uniformly to every wizard, whereas retail's `sub_19D70`/crab loops carry no `+24` owner check (the m9 mound and m15 guard loops DO, ported verbatim) — a kept, harmless extension; (2) the m9 mound's `+80` castle-footprint reach extension (:24201) is not carried into the shared chase (pre-existing; m9 already acquired castles through its dedicated hunt). `rival_wanted` joined the snapshot (version 6→7) and the state hash (goldens re-pinned layout-only — no golden run flags a rival wanted).
- **rivals.rs module header / World::rival_damage_intake (duel pull)** — The duel pull on the CASTER is applied through the knock channel, with magnitude from the traced formula. Deliberate approximation of retail's transport.
- **World::rival_cast_castle (upgrade token)** — RETIRED 2026-08-20 (the graded-lane rule): the "cosmetic" (9,10)→(10,43) ball ride is two graded entity rows per upgrade (mc1l5 t=5152, recycled slots 737/790), so the ch5-mail shortcut is gone. The rival's upgrade now runs retail's cast chain — the commit arms the token (+48 = +50, :19196), the castle token's own machine `sub_57610_57B40` fires the ball from the wizard (banking the +326 charge meter), and the (10,43) delivers the same ch5 level-up mail (`Gen::rival_castle_token_tick`; pinned by the mc1l5 t=5151 fixture).
- **World::rival_cast (castle-stored unlock ladder placement)** — Retail keeps the castle-stored unlock gate (`sub_55DD0` :64917-19) OUT of the AI's cast-readiness (`sub_15A00` :19219) and the picker (`sub_16030`): a rival picks and *fires* a castle-tier spell it hasn't unlocked, arming the projectile in `sub_155F0`, which then fizzles on its first tick (:65049 — disarm, no spawn, fail buzz 29). The port had wrongly folded the gate into readiness, so an owned-but-unaffordable-by-castle spell read affordable-by-ceiling forever and the picker parked on an eternal WAIT — rivals "followed the target and cast nothing" (worst on rich rivals: every castle-tier spell above Fireball in the pick order stayed permanently gated). FIXED by matching retail's placement, with two intentional micro-approximations of the projectile-fizzle: the gated cast is caught at EMISSION (before arming the manifestation / debiting / emitting) rather than one tick later at the armed projectile, and it is SILENT (retail's buzz 29 would storm at Lightning's 1-tick recast; the human keeps its UI buzz). The recast cooldown is still consumed (so the picker's `sub_15E90` cooldown escape advances past the fizzled spell to a castle-free one), and no mana is debited (matching retail's gate-fail, which bails before the debit). Net observable = retail: rivals cycle their whole arsenal, castle-tier spells do no damage until the castle stores enough. Pinned by `castle_gated_spell_does_not_freeze_the_picker`. PLAYTEST OWED (rival combat feel; and whether retail shows a brief projectile flash on the fizzle — unmodeled here).
- **World::rival_pick_mana_target (mana-hunt offense gate)** — Retail's mana-holder hunt (`sub_14B10` :18650) opens with the offense-spell gate `if (!sub_16920(a1)) return 0;`: a rival that owns no offensive spell never enters HuntMana. The port omitted it, so an offense-less rival still locked onto a mana creature and shadowed it casting nothing — a direct contributor to "follows the target, does nothing". FIXED by adding the `rival_has_offense` gate at the top. Pinned by `mana_hunt_targets_creatures_only_and_needs_offense`. NOTE (verified, not a deviation): the scan stays `class64 == 5` (creatures) on purpose — retail walks the PER-MODEL buckets `str_36382[+65]` for model indices 0..=19 (`for i=0; i!=80; i+=4`), i.e. the living-creature models; loose mana balls (model 39) and dwellings (model 45) live in higher buckets the loop never reaches, so the mana-hunt does NOT target them (balls are the Possess/ball-claim path). An earlier attempt to "broaden" this to all mana-bearers was a misread of the bucket index (model, not class) and was reverted. `class64 == 5` slightly over-approximates models 0..19 (a class-5 creature with model >= 20, e.g. hydra m27, is outside retail's scan) — immaterial while such creatures carry no mana; banked.


## mgc-sim — flight & controls (lib.rs, flight.rs, mc1/mobs.rs)

- **crates/mgc-sim/src/lib.rs::ThrustModel::Enhanced** — An enhancement-class thrust model alongside the faithful MC1 mover: hold-to-fly with automatic deceleration on release, generalizing the original's own hold-to-move strafe to the forward axis. Keeps the authentic level-plane thrust rule (aim pitch never steals horizontal mobility; vertical is the altitude model's law — see the move_enhanced entry). Selected once at the sim boundary; replays record it.
- **crates/mgc-sim/src/lib.rs::move_enhanced (chase-the-pointer steering)** — Enhanced steering is chase-the-pointer (player design 2026-07-23, Gothic-3 precedent; supersedes the one-session turn-rate damper, which collapsed into all-or-nothing because a mouse reports displacement, not velocity): the mouse moves the DESIRED heading — the aim crosshair, clamped ±0.6 rad so it stays on-screen at any aspect — and the carpet turns to chase it at rate ∝ remaining lead (CHASE_GAIN 5/s, ~200 ms time constant), capped at ≈ the classic model's own max turn (2.4 rad/s ≈ 2.33 retail). Max-rate through big leads, ease-out on arrival, dead stop once closed; the desired heading is world-pinned between mouse motions. Casts, the crosshair overlay and the autoaim preview all run along the CROSSHAIR (yaw+lead) — aim is instant while the hull catches up (player ruling); movement thrust stays on the hull heading. The yaw twin of the desired-altitude law. RETAIL FACT (player-confirmed): retail turn rate is velocity-independent and our classic mover is faithful to it — chase steering is the opt-in departure, motivated by retail turning being "genuinely silly". Pitch stays direct look (phase 1; the off-desired pitch assist is a banked phase 2). App companions (2026-07-23): the crosshair is a GAMEPLAY option (`render.preference.crosshair`, C, DEFAULT ON — both retails steered by a live on-screen cursor, and chase steering is unreadable without it) split from the autoaim lock markers (`render.debug.autoaim_hints`, debug, off; a stale legacy `render.debug.crosshair` key is IGNORED — deliberately no serde alias, because the menu-persist merge leaves both keys in a migrated mgcarpet.json and an alias turns that into a "duplicate field" load error); the crosshair renders at FRAME rate by adding the sim-unconsumed mouse accumulator to the tick-rate lead and re-clamping against the interpolated camera (`mgc_sim::LEAD_MAX` is public for exactly this), so the pointer doesn't step at 24 Hz while the camera glides.
- **crates/mgc-sim/src/lib.rs::AltitudeModel::ExtendedLift (desired-altitude law)** — An enhancement-class altitude model, reshaped by the 2026-07-22 rulings. The faithful model is terrain-follow only (no fly-up control exists). ExtendedLift: q/e pin a GROUND-RELATIVE desired altitude (ruling 1 — at the cap the carpet maintains 1024 over terrain); the carpet drifts toward ground+desired at the game's standard passive-descent rate (8/tick MC1, the row |buoyancy| MC2) and q/e step at the classic model's max full-pitch climb rate (56/tick, speed-spell scaling ignored). Unified q/e rule (ruling 4): the key always steps the carpet; desired follows via max/min, so the toward-key while lagging is a pure boost and the away-key SETS desired. Desired is PINNED through flings/pins/catapults (ruling 6; reset to the spawn offset on respawn, re-seeded at level hand-off/model switch). Band: MC1 128..1024, MC2 clearance..row band — 3072 in caves per the decompile's row 104, with the cave roof still a separate dynamic clamp; wall blocking fully intact. Skipped while dead/falling (gravity owns the corpse) and under the MC2 paralyze web (the −51 settle is the faithful law).
- **crates/mgc-sim/src/lib.rs::FlightInput::full_stop** — The Backspace full stop is faithful under MC2 (retail action 0x27: zero actual+target speed, kill the Speed/Accelerate channel, recenter steering). It is enhancement-class in MC1/HW, where retail's Backspace is text-entry only. The enhanced turn damper recenters with it (and on death), as steering state.
- **crates/mgc-sim/src/lib.rs::Flyer::roll (camera bank)** — The faithful movers publish the filtered roll stick at full value (retail renders `u16_327` unhalved). The enhanced mover banks ∝ turn_rate × signed forward speed, clamped ±0.6 rad (player ruling 5: physically-motivated but simplified, tuned by feel) — camera roll only, HUD/crosshair stay level, zero when standing. Playtest amendments 2026-07-23: banking is for forward/backward MOTION only; BANK_SCALE retuned 0.030→0.015 (0.030 slammed every turn to the clamp; a max-rate cruise turn now banks ~11°, the clamp is reserved for Accelerate-boosted turns). Amended again 2026-07-27: the "forward/backward MOTION only" rule is now enforced by the forward-velocity projection ALONE (`v·fwd`), NOT a per-strafe on/off gate. The earlier gate zeroed the whole bank whenever a strafe key was held — but forward flight and strafe are simultaneous, so tapping strafe mid-turn snapped the view level and releasing snapped it back (a jarring jerk the player reported). `v·fwd` excludes the instantaneous strafe-aligned velocity by orthogonality, so the sideways drift is ignored without disabling the bank; a pure strafing turn keeps only the mild bank its momentum genuinely rotates forward (~7° in the 40-tick probe, vs the 8.5° of an equivalent forward turn). RETAIL FACT (player-confirmed): retail banking is FIXED and velocity-independent (banks even turning in place); the conditional velocity-proportional bank is the point — "an actual flyer, rather than a toy".
- **crates/mgc-sim/src/lib.rs::FAITHFUL_CRUISE_TPS / ACCEL** — The Enhanced deviation changes only the control response (hold-to-fly vs. accelerate-buildup), never the speed ceiling. Its DRAG-governed terminal is pinned to the faithful cruise (80 engine units/tick) rather than tuned independently, so fixed-size hazards (kraken buffet, tethers) stay proportionally correct under the deviation.
- **crates/mgc-sim/src/lib.rs::move_enhanced (level-plane thrust) + flight.rs module header** — The enhanced hold-to-fly mover (float-based, in lib.rs) obeys the level-plane thrust rule: thrust and the Accelerate override act in the yaw ground plane at full magnitude however far you aim up or down (aim pitch never steals HORIZONTAL mobility). AMENDED 2026-07-19 (player-directed): vertical motion belongs to the altitude AXIS, on both thrust models. Under Faithful altitude the enhanced mover runs the faithful vertical law — pitch-driven climb/dive with the climb-authority band (raw dive; climb scaled by authority, inverted above the ground+band soft ceiling) plus the game-keyed passive decline at any speed (MC2's always-on row-0xe buoyancy above the clearance band, MC1's speed-0 sink above the band). This closed the crosstalk where the forward-flight decline existed under classic thrust (bundled in the ported movers) but not under enhanced thrust + Faithful altitude.
- **crates/mgc-sim/src/lib.rs::move_mc2 (ExtendedLift MC2 arm)** — The MC2 arm feeds the desired-altitude law with the row buoyancy the faithful mover ALREADY applied, compensated inside the law so NET rates are uniform across games (the drift-down simply rides the buoyancy — the game's own standard descent). Floor is ground+256 (MC2 clearance) and the cave roof re-clamps after so the deviation can't pierce it.
- **crates/mgc-sim/src/lib.rs::lift_ceiling / lift_unclamped (`dev.lift_unclamped`)** — The absolute lift ceiling (highest terrain + the ground+1024 soft-ceiling band). Everyday enhanced-altitude climbing caps GROUND-RELATIVE at the per-game band; this ceiling backs the rising-cap safety on the float arm and the `dev.lift_unclamped` instrument (an options-menu Dev toggle, live-applied — the player write-up's "optional debug toggle: unrestrained climb to the current maximum"). Never a god's-eye view either way.


## mgc-sim — sim top level + small MC2 modules

- **mc2/morph.rs::Gen::mc2_summit91_tick** — the apocalypse mana-rain (10,91) skips spawning its three per-tick collectible spheres whenever the free pool has ≤ 200 slots. Retail has no such cushion and spawns unconditionally (relying on the fail-open pool exhaustion). The 200-slot cushion is a deliberate pool-exhaustion belt to avoid starving the pool during the endless apocalypse rain; the per-140-tick decay channel already bounds the rain at ~420 live spheres so the cushion rarely bites.


## mgc-sim — MC2 mobs + multipart

## mobs.rs

- **mobs.rs module header (deliberate approximations)** — The human wizard lives OUTSIDE the entity pool; wizard/class-3 scans visit it first via `MobCtx` (retail's slot-ordered list has the human in slot 1), then pool wizards. The archer arrow's impact effect (`sub_10C80`) is approximated by a channel-0 area-damage write of `f44` through the shared mailbox writer at the impact point (same observable). The arrow's hit probe (`sub_10780`) uses the shared tile-chain victim scan. `TransformEntityToManaSphere` spawns spheres through the MC1 (10,39) ball ctor and writes MC2 launch fields into the MC1 ball's field homes so the shared ball tick flies them, until MC2's own (10,39) handler is diffed. `sub_20130` (archer base+6) is missing from the decompile and stubbed as hold-state (unreachable for archers).
- **Gen::mc2_arrow_tick** — Arrow target-class filter approximation: arrows only strike WIZARDS (xtype=3). Retail keeps scanning the tile ring past a non-matching body within the same tick; the port instead lets the arrow fly on and re-probes next tick.
- **Gen::mc2_alliance_convert** — Retail also converts stage-HELD creatures (StageVar1 saved to `word_0x4A_74`, restored on expiry); the port skips creatures under a live hold or another charm.
- **Gen::mc2_alliance_clock** — On combat resolution retail returns controlled creatures to their `8m+7` controlled slot; the port's per-model state machines drop them to their wander phases 0/1 instead.
- **Gen::mc2_alliance_creature_tick** — Retail adopts the parent wizard's target/attacker words; the port's out-of-pool human keeps neither, so the observable equivalent serves: target the nearest pool entity currently targeting the parent, else the nearest enemy wizard.
- **Gen::mc2_proj_tick** — Non-(9,13) MC2 projectile states fall back to the MC1 projectile handler. With the MC2 spell column landed, live player casts stamp `F_MC2PROJ` and no longer reach this fallback; it survives only as the graceful-degradation contract for any non-(9,13) state that slips through (deliberate cross-column seam).
- **Gen::mc2_spawn_building** — VGA half-resolution footprint shrink (low-res render mode) skipped; `dword_0x10_16 = 2` has no ported consumer; the id-68 player-castle global is deferred to MC2 castles.
- **Gen::mc2_building_tick** — The one-at-a-time build carousel is skipped: all authored buildings raise concurrently at load (the retile / texture-band paint / pad-edge rings run at the retail cadence).
- **Gen::mc2_house_tick** — The `byte[2]&0x20` strong-claim lock is not modeled (all claims run the weak possess variant). (AMENDED 2026-07-23: the old second clause — "the claimed sprite-row colorize rides the renderer's team tint" — was wrong twice over: the billboard pass has no team-tint stage, so the flag was stuck on the bare 177 row and rival-claimed dwellings flew the HUMAN's flag. Now ported faithfully: retail's `+90 += TransformPlayerColorIndex` after the sprite set = flag row 177 + owner color, extents derived off the base row.)

## multipart.rs

- **multipart.rs module header (deliberate approximations)** — `struct_byte_0xc` group markers (m27 byte[2]/byte[3], the m22 byte[2]|=0x20 sound split) are not modeled. The m27 segment show/hide writes flags bit 0 verbatim PLUS the port's 0x20 draw alias (the renderer's billboard skip) rather than widening to retail's 0x21 globally (which would break the MC2 map-only house pose and the cave balloon). (AMENDED 2026-08-03: the 0x21 skip IS now honoured for the MC2 **class-5** column — `World::live_poses_mc2`'s `hidden` — because the doomsday pyramid's ctor bit 0 is the boss's whole dormant/corpse invisibility, and no other MC2 class-5 ctor writes bit 0, so the widening is provably scoped. It is still NOT global: several ported class-10 effect ctors set bit 0 faithfully — (10,19) fire spray, (10,23) `sub_4F5F0` EF:36101 after `SetEntityIndexAndRot(7)`, (10,25) `sub_4F6A0` EF:36121 — while the port draws their billboard as a STAND-IN for the retail `AddEvent2_847D0` particle child it does not implement, so widening there would make impacts and ground fire go dark.) `byte_0x5D_93` palette-shade is renderer-side and unmodeled.
- **Gen::m0_tick / m3_tick (states 0x06/0x1E)** — `sub_1F2B0` and `sub_1FA40` were disassembled from the shipped binary: both are compiled empty stubs (AUDIT-STUBBED-ARMS-2026-07-26.md), so the no-op arms are faithful, not deviations. (The former entry here claimed the m0 `sub_1F0C0` tether was dormant — FALSIFIED: `sub_68BD0` EF:55463 arms it from the class-9 acquisition lock; the dodge is now ported at `Gen::m0_dodge`. m3 has no dodge: the arm is model-0-gated and m3's recovered states never call the tether.)
- **Gen::m27_branch_bolt / m27_drive_branch** — `sub_2A7F0`'s low-power path perturbs the branch LCG by the global `setting_30` game-loop counter; unmodeled, so the branch stream diverges from retail after the first bolt roll. `sub_2A940`'s `x_DWORD_E9BA8` freeze gate reads as 0 (the normal path).
- **Gen::m27_tick (emerge/teleport)** — The `sub_102D0(_, _, 4)` second capability-mask probe is folded into the shared `mc2_path_blocked` (a3=1 arm) + roughness test, like the shared move core.
- **Gen::m22_tick (castle-drain 0xB2/0xB3)** — Reaches through the target player's `CastleEntityIndex_0x3A_58`. LIVE since rival castles landed: a possessor WITH a castle receives the full retail drain — the worm chases, delivers its mana to the castle, and self-consumes (vanishes; `sub_26BD0` EF:17373-17414, faithful). A castle-less possessor (typically the human) takes retail's own castle-less arm (LABEL_17 revert) and the worm survives.


## mgc-sim — MC2 rivals + doomsday

- **rivals.rs (module) — hate feed** — The rival's hate ledger is fed from the shared damage-intake path (`mc2_rival_intake`, on the mailbox) rather than retail's per-projectile scan `sub_159E0`. Same inputs, slightly earlier; this is the MC1-column position and an APPROX pending a per-projectile port.
- **rivals.rs (module) — DEFENSE disguise visual** — The DEFENSE state's disguise VISUAL (retail draws the metamorph creature in place of the AI carpet) is presentation-side and unported. The state machine, tier pick, shadowing and speed law are faithful (`sub_15FC0`/`sub_161A0`); only the on-screen model swap is missing. Open.
- **rivals.rs::mc2_rival_buffs (Heal channel)** — Rival Heal (spell 5) heals at an APPROX rate of maxLife/20 per armed tick — the MC1-column certified rate; the MC2 numeric rate is not yet pinned from the trace.
- **rivals.rs::mc2_duel_drain** — The duel life-regen drain term uses the afield /500 rate; retail reads the stored `lifeRegen_0x163_355`, which only differs while the rival sits at its own castle. APPROX.
- **rivals.rs::mc2_rival_pick_ball** — A ball owned by a NOT-hated wizard, when NO wizard exists in the world at all, is skipped (matches the retail quirk). A hated owner's balls rank from the rival's OWN castle; a castle-less rival anchors that ranking to self (retail reads the Entities[0] sentinel there — documented idealization, not the literal null read).
- **doomsday.rs (module) — state timers** — Sprites 343/344/345 auto-size their state timer to the animation length via `sub_221F0`/frame table; the sim doesn't carry TMAPS frame counts, so the seeded timers (16/32) stand. Cadence-only deviation.
- **doomsday.rs (module) — palette flashes** — `sub_5C800` palette flashes (case-7 beam flash 6) are presentation and skipped, like every flash effect.
- **doomsday.rs (module) — projectile burst acquisition** — The (9,3)/(9,26) projectile bursts (selectors 9/8) are pre-locked at the avatar via `mc2_arm_proj`; retail self-acquires on tick 1. Acquisition-timing APPROX in the proj module.
- **doomsday.rs (module) — case-0xE global reset** — Retail's case-0xE wipe writes `byte[1] |= 0x20` (an unmapped, name-inferred render bit) on every entity. The port applies the life/maxLife=140 reset and skips the render bit.
- **doomsday.rs (module) — per-list scans** — Retail's per-list scans (`dword_38531` buckets) are reproduced as pool slot-order scans over the mobs list. APPROX.
- **doomsday.rs (module + mc2_pyramid_do_summon case 7) — HURL-AWAY beam** — The case-7 hurl-away beam displaces the human via the shared knock channel (`Gen::player_knock`) rather than retail's MoveEntity + moveTest + floor clamp on the pose (the app owns the pose). Same observable: violent outward displacement, 944 units on the first push decaying to 10.
- **doomsday.rs (module) — LCG perturb** — The `rand += setting_30` LCG perturb after the two summon-pick rolls is unmodeled (project-wide convention).
- **doomsday.rs (module) — word_0x36548** — The doomsday-active global `word_0x36548` (set case 0, cleared case 0xF) has no reader in retail (savegame/debug only) and is not carried.
- **doomsday.rs::mc2_doomsday_tick (render-arm proximity analog)** — The wind-down escape bit is armed by retail's DETAILED-pass render writer (`subSpellIndex |= 0x40`), which a headless sim can't couple to. Reproduced as a deterministic proximity analog: any radius >= the machine's own 0xA00 far-gate is behaviorally identical (far ticks just re-clear the bit), so the far-gate distance is used.


## mgc-sim — MC2 cast

- **mc2/cast.rs::World::mc2_scatter_spells (the death scatter's LCG)** — Retail rolls the three draws per scattered spell token off the DYING WIZARD's private stream (`a1x->rand_0x14_20`, `sub_5E310` EF:60148-61). The human owns no pool record in this port, so its private stream has no home; a COPY of the token's own seed stands in — same constants, same shape, different scatter offsets. Deliberately NOT the world stream (which would desync every other entity's draws on the landing tick) and deliberately not the token's live `rand` field (retail's scatter never writes it). Costs ~104 field rows on the ONE landing pair per death (26 tokens × life/x/y). Closable by importing the recorded carpet `rand`.
- **mc2/cast.rs::World::mc2_aim_preview** — Retail MC2 draws NO crosshair/reticle; the aim feedback is the sprite-42 projectile visibly curving toward its target (docs/traces/mc2-autoaim.md §4, mc2-mouse-aim.md §4). This method is an opt-in (Preference-class) predictor that returns the target the hand's spell projectile would acquire on its first flight tick — a pure twin of the real aim scan. It is an enhancement, not a faithful surface; keep it gated as an instrument rather than "fixing" MC2 to always show a reticle.


## mgc-sim — MC2 roster + tail

- **mc2/effects.rs::mc2_mine_detonate (blast box, bolt, spent-mine teardown)** — PATCH OPTION `gameplay.patches.mc2_magic_mine` (default patched; was always-on until 2026-08-08 — the retail arm keeps the whole arming/trigger lane dead, and strict-retail replay now skips it too). Retail's post-trigger path is the untraced `sub_6DCA0` relaunch (`spell-audit/magic-mine.md` §6 Q2), and in retail the player could not get a mine to trigger AT ALL (§6 Q1: nothing is known to write the `word_0x36_54` armed gate). Three deliberate choices on the player's ruling, all "better than retail" rather than faithful-to-a-dead-spell: (a) the detonation opens a 1024 (4-tile) blast box for its `area_write` — the ctor sets no extents, so `ent_overlap` summed to a POINT and a wizard beside the mine took nothing; the box is restored afterwards so it does not linger through the sink; (b) the mine SPITS a (9,0) bolt at whatever tripped it, since §5 step 4 describes the detonation as a spell *relaunch*, not a bare area write; (c) the spent mine is handed to retail's OWN expiry teardown (`f71 = 6` → 7 → 9: hang, sink, puff, despawn) instead of vanishing on the spot.
- **mc2/roster.rs::module (+6 states)** — Every `+6` creature state whose body is missing from the decompile (m2, m9, m16, m17, m18, m19, m20 nominal, m21, m23, m25, m26, m28) holds inert rather than dispatching (which would crash in remc2). Retail can never reach these states (the rows' flee bit is clear).
- **mc2/roster.rs::module (m18 sub_253B0)** — m18's `sub_253B0` duration table is only partially pinned: the trace lists the formulas but not the (state,sub)→formula map, so the mapping is approximated.
- **mc2/roster.rs::module (m26 human drain)** — m26's human mana drain uses a flat `+14` because the human's manaRegen isn't modeled yet.
- **mc2/roster.rs::module (m12 site scans)** — m12's site-jitter / footprint-clear scans (EF:13991-14093) are shaped, not verbatim, because the overlap helpers are untraced.
- **mc2/roster.rs::m2_tick (vertical homing)** — m2 vertical homing toward the target's top (:11509-20) uses the human's carpet z as its top because the human's half-height isn't modeled.
- **mc2/roster.rs::m9_tick (grounded consume)** — the m9 grounded-variant consume sweep reuses the shared seek path as its mirror rather than a separate scan.
- **mc2/roster.rs::m20_tick (melee rush)** — retail also gates the human melee rush on the mobilize counter (MC2 flight state); that gate is not modeled, so our m20 commits without it.
- **mc2/roster.rs::m23 descend** — "aligned over node" is approximated as a 2-D closeness test standing in for retail's sub_28390/sub_28060 alignment.
- **mc2/roster.rs::m24 castle approach** — "in range" of the castle is approximated as an AABB box overlap standing in for retail's CompareAxisWithShift_10750.
- **mc2/roster.rs::m28 strike** — the strike animation length is fixed at 16; retail's count comes from the anim bank, which isn't modeled.
- **mc2/tail.rs::module (spellbook hit counts)** — the `sub_6D8B0(id, kind, hits)` spellbook reports ((10,17) kind 9, (10,23) kind 7, (10,15)'s spray kind) compute their hit counts and drop them; the spell-XP intake is emitted by the spell-XP column instead.
- **mc2/tail.rs::module / mc2_fire_spray_tick ((10,19) singleton latch)** — the (10,19) spray's `word_0x33` singleton latch (EF:23962 registers a new spray and disables the previous from a different action's context) has no ported writer, so the on-death release write is a no-op. (OPEN.)
- **mc2/tail.rs::module (AddEvent2 children)** — `AddEvent2_847D0` attached lights/children (e.g. (10,23)'s (128,9,0), the fire-orb satellites' (128,1,0)) are presentation only and left unported.
- **mc2/tail.rs::module / mc2_aura_tick ((10,54) creature list)** — the (10,54) aura scans a pool slot-order list over mobs.rs standing in for retail's `dword_38523` creature list.
- **mc2/tail.rs::mc2_whirlwind_lift (player sway)** — the tornado's pull on the human is approximated: the full grab/lift/camera-roll takeover (the deferred FlightVerb seam) is not ported, so the "funnel drags you in" observable rides the `player_knock` channel as a ~45°-tangential spiral toward the eye. Also: the HUMAN player arm (yaw-step 56, threshold 384, camera roll, actSpeed 80) awaits the FlightVerb takeover seam, so until then the player is damaged when overlapping the eye ring but not lifted; and the victim z-float band (`sub_580E0` row args) collapses to the computed lift z (the row hover clamp needs the behavior rows' word_0xa/0xc homes).
- **mc2/tail.rs::mc2_aura_tick (magnet-path reuse)** — the (10,54) mana-magnet reuses the established magnet path (ball_tick dest drift + merge) rather than retail's homing triplet (target stamp + pull-speed word); the triplet collapses to the same observable motion.
- **mc2/tail.rs::mc2_whirlwind_lift (reap-skip guard)** — a 0x400 reaped-slot skip is added to the victim scan that retail's gate does not have, to avoid acting on reaped slots.


## mgc-sim — MC2 castle + projectiles

- **mc2/castle.rs::module (sub_5F890)** — The Create-Castle HUD build-ghost/widget sync calls (`sub_5F890`, EF:61029) are no-ops: no ported HUD widget exists.
- **mc2/castle.rs::module (owner palette shift)** — The castle's owner recolor (`word_0x5A_90 += TransformPlayerColorIndex`, EF:61139) is approximated by riding the renderer's team tint rather than a palette-index shift.
- **mc2/castle.rs::mc2_castle_roster (slot arrays)** — The balloon/guard slot arrays (`array_0x3C_60`/`array_0x5C_92`) are scan-collected each roster pass rather than kept as retail's per-slot index arrays. Same membership, no per-slot indices.
- **mc2/castle.rs::mc2_castle_downgrade (10% haircut)** — PATCH OPTION `gameplay.patches.mc2_downgrade_overflow` (default patched; was always-on until 2026-08-08). The patched arm computes the 10% capacity haircut in i64. Retail's i32 `10 * x / 100` overflows at the always-overflowing level-7 rung (10 × 300M) into a NEGATIVE cut, so a maxed level-7 castle downgrade *raises* its cap and scatters nothing; the retail arm reproduces the wrap exactly (`wrapping_mul`).
- **mc2/castle.rs::mc2_castle_destroy (eject ordering)** — The downgrade death arm front-loads the balloon→sphere conversion where retail leaves it to the roster call, so balloon-spheres draw before the bank-spheres. Same total mana, different LCG interleave.
- **mc2/castle.rs::mc2_castle_intake / mc2_balloon_tick (HUD alert)** — The "castle under attack" / balloon-under-attack HUD flags (retail per-owner `byte_0x195_405` / `byte_0x197_407`, EF:61752/61947) are a single player-side latch; per-owner records await a rival defense-AI consumer.
- **mc2/castle.rs::mc2_piece_scan (turret target order)** — Retail walks the per-ring cell-offset tables nearest-ring-first and takes the FIRST hostile in walk order; we take the nearest by ring then pool order (same admission set, same 3-tile hole). The class-5 `StageVar2==14` own-parent exemption (EF:30378-81) is skipped (the stage binding lives in side-vecs).
- **mc2/castle.rs::mc2_research_stamp / mc2_castle_part_type** — Castle turret research (part-type + HP factor) is stamped at cast/upgrade time from the castle-spell tier rather than driven by retail's `sub_69AB0` research/production child. The HP factor is recorded but the ladder still runs identity.
- **mc2/proj.rs projectile flight (anti-tunnel chord march) + mc1/combat.rs::Gen::probe_window (the MC2 square window)** — The MC2 projectile mover ray-marches its per-tick chord in ≤128-unit sub-steps where retail probes ONCE at the end of the move (EF:63126-29): a deliberate anti-tunnel deviation, invented because several projectile sprites carry a zero-width collision box that a 384-unit endpoint-only probe steps straight through. Coupled to it, the shared victim scans (`victim_scan`/`claim_victim_scan`) keep the port's OLD window geometry on MC2 only: a truncated-centre square with a `.max(1)` radius floor, where retail walks the SEARCH.DAT ring iterator over the rounded centre (sub_10780/sub_108B0 — byte-identical to MC1's sub_11980). Measured 2026-08-12/13: the march and the inflated window are ONE compensating family — giving MC2 the retail ring under the march cost five pinned fixtures (fools-trap muzzle, meteor homing lock, arrow collateral direct hit, two muzzle-admission guards) and mc2l4 t=621, and it costs mc2l4 +1 / mc2l30 +2 conforming pairs to keep them (forfeited, documented at the `probe_window` seam). MC1/HW run retail's exact ring geometry — their movers are endpoint-only like retail, so no compensation exists to unwind (landed 2026-08-13, mc1l0 −114 rows). Un-parking this means removing the march and the square TOGETHER and re-litigating the five pins plus the zero-width-box tunneling class.
- **mc2/proj.rs::mc2_proj_impact (unported effect)** — An impact whose (f68, f69) effect is unported applies its f44 as channel-0 area damage at the impact point (the effect IS the damage carrier in retail) and counts the misfit; damage lands, the visual gap stays in the ledger.
- **mc2/proj.rs::mc2_flyer_tick (no-target snapshot)** — A target-less flyer snapshots its aim once (the retail else-arm, EF:62914-16) and flies straight; the model-keyed acquisition sweep (`sub_67CB0`) serves player-cast spells only.
- **mc2/proj.rs::mc2_rebound_deflect** — The human deflector's mana debit is skipped (the wizard ledger is world-side); the returned bolt's xtype/xsubtype re-key (EF:55299) is ported for the human shooter only (a pool shooter id has no O(1) slot resolve). Pool victims deflect on the authored 0x8000 shield bit and always scatter — rival Rebound windows are not yet mirrored onto their entities.
- **mc1/combat.rs::proj_m1_tick (m17 dwelling touch-claim — reconstruction bridge)** — Retail's magnet bolt claims a dwelling it passes through in flight (player retail-verified 2026-07-27, exact-hit and pass-through alike). The reconstructed chain cannot do this at all: the (10,12) flash writes channel 1 while the (10,45) ctor's +28=33 listens on channel 0 only, and an exhaustive sweep of the state-18 flight (sub_11C00/sub_11AC0 pure scans, steer/move/flash-"mover" all side-effect-free) proves the claim write was reconstructed away — the same gap applies to plain possession's house claims. The port bridges with an in-flight direct ch1 mail on overlapped dwellings (gated like the possess scan's :17067 — not own by +24 or +144), riding the port's built-house ch1 intake (`building_fixup` f28|=2). Mechanism APPROX (channel and write-site invented); observables match retail. Pinned by `mana_magnet_pass_through_claims_a_dwelling`.
- **mc2/proj.rs::mc2_aim_scan (acquisition approximations)** — The owner lock range rides the shared wizard-row v_28 (4096); bucket 22 (worm family) is approximated as model-22 heads + their f54 chains; the cave-in on-ground filter is z within one step of terrain. The offensive branch's EF:54788 self-self distance is a decompile artifact — the correct two-point form is used instead.


## mgc-sim — MC2 stagevars / flood / terrain paint

- **stagevars.rs::set_mc2_stagevars (0xFF fill rows)** — retail's slot-count scan would include a 0xFF editor-fill tail and load it with a garbage out-of-table subtype read; the port treats the 0xFF fill as an empty slot. No shipped level can deliberately bind through the garbage read.
- **stagevars.rs module header — APPROX register (held reductions)** — the per-model phase-7 wrapper EXTRAS around retail's `sub_1D5D0` (ambient-sound draws + speed refresh, e.g. the goat's `AddGoat05_01_1F5B0` bleat, m18's ground re-snap) and the `sub_1EEE0` settle on the walk leg's hit path are not run — no idle SOUND rng is drawn while held.
- **stagevars.rs::mc2_stagevar_tick (deferred m9 arm)** — retail arms a deferred m9 (hive-imp) hold inside the materialize-completion tick (EF:11984-95); the port's pre-loop pass arms it one boundary later. Same observable sequence, memimage-corroborated on mc2:04 (39 held m9s flip at the same tick as the retail dump's frame 16).
- **stagevars.rs::mc2_stagevar_tick (kind-9 proximity fallback)** — retail's kind-9 "proximity fallback" (`sub_12500` EF:5108-12) reads the row union as world coordinates and releases a held creature within 12 tiles of that "point" on BOTH axes. The union actually holds the death-watch entity pointer (pre-autosave) or its serialized offset (post-severance: `(slot·168, 0)` — y pinned to the map's top edge for every slot < 390), so the box never matches a real hold position in either era (2026-07-25 audit: provably inert both ways). The port does not reproduce the branch.
- **stagevars.rs::mc2_stagevar_tick (&2-clear death watch = the AUTHORED law; shipped retail is an autosave-severed coin)** — the shipped binary implements a REAL death watch: pass-3 (`sub_12100`, thing-index keyed, divisor 20 — instruction-proven) binds a live entity pointer, and `sub_12780`'s else-arm fires when that entity's `life < 0` or dying flag sets. But retail campaign play never keeps the law past the first seconds: a one-shot in-level checkpoint autosave (`sub_57640` → `SaveLevel_55080` → `sub_55100`) serializes the pointer to a slot offset IN PLACE, and the restore pass is structurally unable to convert it back (the pool-bounds gate reads the offset as a pointer and always rejects it), permanently severing the watch; the subsequent raw deref of the offset lands in DOS low memory, making the gate a PER-CONFIG constant — fires at ~the autosave moment on some machines ("archers march at load", the player's own setup, live-capture-verified: FIRED with all skeletons alive, union = 0x7230) and never on others ("crater camp"). PLAYER-ADJUDICATED 2026-07-25: the port implements the AUTHORED level-data semantics (held while unbound or while the bound entity lives; fire on its actual death) rather than either side of the shipped coin. On mc2:04 the archers hold the crater until the vanguard skeleton dies — by which time the skeletons have reached them. Affects ~30 shipped rows across ~20 levels. Full evidence chain: docs/traces/mc2-level004-stagevar-ground-truth.md.
- **stagevars.rs::mc2_held_watch (kind-4 join arm NOT ported)** — retail's kind-4 "join the watched entity's fight" arm (`sub_1D700` EF:10022-66) is inert in shipped retail for a now-recovered reason (2026-07-25 binary audit): for a `&2`-clear row it resolves the watch through the row union as a raw pointer behind a LOWER-bound-only guard (`> Entities_EA3E4[0]`). After the in-level checkpoint autosave severs the union to offset form (~tick 5-15, every level), the offset passes the null check but FAILS the lower bound → guaranteed early return; the held creature keeps the shadow walk and never adopts a target. (In the brief pre-autosave window the arm CAN reach the deref, where the watched idle creature's `word_0x96_150` is uninitialized garbage — remc2's `if (v4 == 0xae02) return;` bandaid marks junk its level-5 replay hit there; the shipped binary has no such sentinel.) Player retail replay on mc2:04 (2026-07-24): the worms crawl along with the skeleton column and never join the battle. A clean-data port of the arm made the worms attack the archers. Kind 4 keeps the shadow walk, held-hit retaliation and the fired-bit release. NOTE: under the port's data-faithful death-watch law the union stays a valid binding, so a clean-semantics port of this arm would genuinely fire — kept unported per the player-certified retail observable.
- **mobs.rs::archer_brain (Scan B unnatural fallback)** — the decompile's archer acquire Scan B walks only the model-9 chain (`bytearray_38403x[36/4]`, :11811), but retail-replayed mc2:04 (2026-07-24) shows the archers turning on the WORMS the moment the skeletons are extinct — the monster-hunter design ("archers target unnatural creatures"); the retail mechanism is unrecovered. The port's Scan B falls back to the next unnatural model (m3 worms) only when the previous one is EXTINCT, preserving the battle order exactly.
- **stagevars.rs::mc2_stagevar_release (immediate rescan on gate release)** — retail's mc2:04 archers attack the worms the instant the skeletons are extinct; the port's released creatures waited up to `4*v_26` ticks for the next idle acquire scan. The port zeroes the released creature's `f63` phase counter (reaction-path releases only; spawn-time cadence-skips keep their ordinal) so the same tick's brain run passes every cadence gate. The retail mechanism is UNRECOVERED — a port-invented nudge matching the observed timing; re-check against a true NETHERW.EXE decompile.
- **stagevars.rs::mc2_held_hit** — retail follows the hold-break-to-aggro with a `sub_1EEE0` ground settle; the port skips it.
- **stagevars.rs::mc2_held_tick (predator/guardian speed tails)** — only the FLEE-prey (goat/townie) +7 wrapper speed tail is ported; predator/guardian wrappers (m18/m19/m21…) keep their spawn speed while held, their per-model tails and sound rolls skipped.
- **flood.rs module header — spell-XP / objects counter** — `sub_6D8B0(id,0x14,n)` spell-XP reports (EF:29367/:29436) have their counts computed and dropped except the player push; the global objects-hit counter `x_DWORD_E9B90` (EF:28527) has no ported reader.
- **flood.rs::flood_shove (human player arm)** — the player lives outside the entity pool, so retail's class-3 model-0 shove is approximated: the horizontal pull rides the `player_knock` channel, the z pull-down and pitch-512 spin bank on the FlightVerb takeover seam, and the close-range 1-in-7 kill mails a kill-scale 32000 (retail adds the victim's `life+1`, unreadable for the player).
- **flood.rs::flood_shove_hit (rival-wizard spin)** — the class-3 model-0 pitch-512 spin (body-flip presentation) is skipped; the damage roll is faithful.
- **flood.rs::flood_shove (action-74 visibility juggle)** — retail's local-player draw-latch juggle (EF:29118-29127, byte[0] bit0 set for the local wizard, cleared for everyone else) is collapsed: the port clears `F_TOSSED` for everyone (single-player observable: victims become shoveable again).
- **flood.rs::flood_shove (deep-sink skip)** — retail's deep-sink skip (`word_160_0xe_14 < -64`, the victim's z-velocity, EF:29106) has no ported home; the z pull always applies before the ground clamp.
- **flood.rs module header — mana field** — `mana_0x90_144 = 0` in phase 1 (EF:28548) has no ported reader on this column and is skipped.
- **terrain_paint.rs::mc2_smooth_pad_edge** — the packed-word neighbour index is reproduced with wrapping u16 (torus) math like the rest of the port. Retail's gate reads signed-negative offsets near row 0 (remc2's fix pins those to 0/natural); the torus wrap reads the far edge instead. Divergence only for footprints touching row 0.


## mgc-app — main.rs

- **map edge-scroll (the whole letterbox bar scrolls)** — PLAYER-RULED. Retail scrolls the map when the confined cursor sits on the exact screen-edge pixel (MI:3132-75). Letterboxed, that edge is the PICTURE's, and the pointer can rest anywhere in the black bar beyond it — an edge-only trigger would leave a dead strip where the cursor is off the map and nothing happens. The test reads "at or beyond the edge", which takes in the whole bar on whichever axis is barred. Retail had no bars, so there is nothing to be faithful to.
- **App::new (no menu music under a launch intro)** — The campaign frontend normally starts its menu track (`csetup` / the MC2 SETUP render) at boot. When the intro chain is about to play, it does not: the movie owns the audio from its first frame, and starting the menu track only to stop it a frame later was audible as a blip of menu MIDI under the opening (player-reported). The chain hands back to `enter_main_menu`, which starts it properly.
- **main.rs::WorldInit::prune_owned_jars** — removes spell jars the local player already owns at every level load (the sim self-culls owned jars on their next tick). Preference-class improvement over retail, applies to both MC1 and MC2.
- **main.rs::load_level `--pool-slots` (chassis.pool_slots)** — dev flag bumps the entity pool beyond the game's pristine profile; limit-removing override, G-class, a run under a bumped pool is not a faithful fixture.
- **main.rs::load_level `--awake-range` (chassis.awake_gate_sq)** — dev flag overrides the creature awake gate (faithful = 24 tiles; 0 = always awake); G-class, not a faithful run.
- **main.rs::WorldInit::placeholders** — draws stand-in art for unported MC2 models (default on MC2 until its roster closes).
- **main.rs::App::quit_fade (end-of-game ending)** — on level WON the app fades to black and exits with no stats screen and no menu return; deliberate simplified ending (both games leave through the same door; campaign mode routes to the next level instead of exiting).
- **main.rs::App::tick_input (fly_assistant)** — the virtual-stick recenter (retail MC2 "fly assistant") is a Preference option defaulting OFF; MC1/HW never had it (enhancement-class there), and a parked cursor's deflection persists as retail MC1's visible-cursor scheme did.
- **main.rs::App::exit_confirm** — the retail MC2 "Abandon level?" OK/Cancel dialog is also shown for MC1 and single-level play, which retail left unguarded; deliberate extension so an accidental Esc cannot discard progress. Modality stays retail-faithful (world keeps running beneath the dialog).
- **main.rs::App RedrawRequested (view_pitch/view_roll)** — the enhanced (mouse-look) flight camera renders flat: no horizon bank from the roll stick, unlike the faithful camera which renders the full filtered roll (remc1 :52432). Deliberate for the mouse-look control model.


## mgc-app — ui.rs + config.rs

- **crates/mgc-app/src/ui.rs::UiAssets::hud_notification_anchor** — anchors the top-of-screen notification toast to the LIVE HSPR sprite geometry (below the info-boxes, right of the radar cap) instead of retail's 320-native `132,50` literal, which was authored against the half-size MSPR strip and doesn't map onto the larger HSPR panels; keeps the toast placed correctly at any resolution.
- **crates/mgc-app/src/ui.rs::book_quads (quick-select badge)** — the number badge on a hotkeyed spell's book cell is kept ALWAYS-ON; retail gates it on a per-spell countdown (+844, decremented per draw so the badge flashes after assignment) or a book-wide flag (+14421). Chosen as the more readable interpretation.
- **crates/mgc-app/src/ui.rs::hud_quads (win_tick)** — the MC2 mana-goal tick reuses the MC1 marker: WHITE one blink frame, draw-skipped (transparent, the pixel below shows) the other. Retail MC2 alternates its own two team-ramp entries instead, reading white/blue-ish (GameUI.cpp:212-17); geometry and phase match, only the ramp differs (player-ruled 2026-08-21). The former GREEN "completed" recolour deviation is retired — the marker now blinks the retail way with no completion recolour.
- **crates/mgc-app/src/ui.rs::exit_confirm_quads** — presentational additions to the in-level abandon dialog: a mild hover tint on the OK/Cancel buttons and a soft slab behind the prompt text for readability over bright terrain. Retail's only feedback was the cursor itself, and its palette font carried its own contrast.
- **crates/mgc-app/src/ui.rs::vitals_quads (spawn-grace shimmer)** — a thin white bottom-center strip that drains with the respawn-invulnerability window; retail shows NOTHING for grace. Behind `render.debug.grace_meter` (a debug cue, default off).
- **crates/mgc-app/src/ui.rs::roster_quads (MC1 kill-matrix compaction)** — the map-roster's kill-matrix columns COMPACT when a wizard's row is removed (elimination), in both games. MC2 retail compacts (EF:22318-56); MC1's decompiled column loop advances its cursor unconditionally (:27100-24), which would leave a one-column HOLE at an eliminated wizard's column — unverified against a retail mid-level elimination and reads as a bug, so MC1 follows the MC2 law (player-ruled 2026-07-24). A retail replay showing the hole would make this a one-line revert (advance `x` in the None arm).
- **crates/mgc-app/src/ui.rs::selector_quads (corner tags = ring ∪ equipped, always-on)** — retail keys the L/R corner tag on CYCLE-RING membership only (`array_0x3B5`, EF:22546-53); a spell equipped via the plain pane commit (which never joins the ring) shows no tag. We draw the tag for ring members OR the equipped hand, and on EVERY box rather than only the hovered one (player-ruled 2026-07-24: the notches must be visible always; the remc2 decompile gates them on `spellOnCursor_50` == hovered — player experience outranks the decompile here). The MC1-art pane wears persistent corner TRIANGLES pointing into their corner, translucent white (0.5 = equipped, 0.25 = ring member; player-tuned) — that surface is wholly our idiom.
- **crates/mgc-app/src/main.rs::MouseWheel (wheel_spells)** — mouse-wheel spell cycling (wheel = LEFT-button ring, SHIFT+wheel = RIGHT), the remc2/MC2HD idiom; retail binds nothing to the wheel. Additive input, default ON, `--no-wheel-spells` / `gameplay.enhancement.wheel_spells` to disable.
- **crates/mgc-app/src/config.rs::GameSpeed::Slow** — a half-speed (0.5×) game-speed level with no retail equivalent (retail's slowest is Normal); for sightseeing/accessibility. F3 cycles it in alongside retail's three.
- **crates/mgc-app/src/config.rs::RenderPreference::fog_distance** — default is 50 tiles, not retail's 20; retail's 20 was a pure period-performance choice. Menu stops offer 20 (faithful) / 50 (default) / 100 / 255.
- **crates/mgc-render/src/terrain.wgsl::ext_amount (silhouette extinction, round 2)** — terrain past the fog wall stays visible as a flat fog-constant silhouette against the textured sky (the whole 3x3 torus field rasterizes; retail instead hard-culled at its 20-tile cutoff and never had the band). The silhouettes are KEPT (player-ruled 2026-08-08: "makes for a nice scenery"); a second ramp melts them into the actual sky-texture pixel behind them (`sky_backdrop`, the sky.wgsl ray law) across the FIXED band 95..125 tiles (EXT_START..EXT_END), then discards — fully gone before the ~128-tile half-map distance where a peak's nearest torus copy switches sides (the player-reported sharp appear/vanish pop), with the far-field fill-rate recouped. Round 2 (player-ruled): the band is FIXED and UNCONDITIONAL — round 1 anchored it to the fog wall and gated it off at high stops, entangling the two systems; instead the fog setting itself is capped at 90 tiles (`MAX_FOG_TILES`, clamped at config load + CLI + renderer; the old 255 whole-torus menu stop is retired) so the fog band always ends short of the melt. SPRITES follow the exact same law (billboard.wgsl): fog-colored cutouts past the wall like the landscape, melting across the same band — round 1's full-fog discard made sprites pop in/out of existence at the wall (player report). With no sky loaded (caves, sky off) the melt target is a 1x1 texel of the fog constant — the plain constant fade, bit-equivalent to before.
- **crates/mgc-render/src/terrain.wgsl + billboard.wgsl (mirror-arm fog target = mirrored sky)** — in the water-reflection MIRROR pass, fog fades toward the mirrored sky-texture pixel behind the fragment instead of the flat constant — for terrain AND sprites. Retail's law is the constant in every pass, but the mirror's backdrop is never the muted horizon rows the constant was authored against — it is the elevation-negated sky, the bright cloud band — so the constant fade left full-contrast cutouts in reflections, presented crisp in NEAR fog-free water (player reports 2026-08-08: "fog never applies to reflections"; round 2: mobs and castle flags "very clearly visible in the mirror well before visible in the normal plane" — from carpet height a far object's reflection lands in close water at full sheen strength). Reflected content now melts into the reflected clouds at the same rate the direct view melts into the horizon; fully-fogged mirror fragments discard (the mirrored sky pass already painted that exact pixel).
- **crates/mgc-render/src/blur.wgsl (reflection gaussian blur)** — the water's mirror image is softened by a separable 5-tap gaussian at 1/2 resolution (H into A scaling its taps to the destination pitch, V into B; the water fragments sample the blurred B, plus free bilinear-upsample softness) before the sheen blend. Player ask 2026-08-08: "the reflections have always been kind of too clean… some plain gaussian blur for anything passing through the water level"; round 3 halved the diameter from the first 1/4-res build ("roughly half… would be great"). Fidelity note: retail's reflection block rendered at 320x200 and was inherently soft — a pixel-perfect modern-res planar mirror is the less faithful *feel*. Always on with reflections; strength = `REFLECTION_BLUR_DIV` (lib.rs, must match blur.wgsl `DIV`) + the kernel. Runs only when the mirror pass runs (two 1/4-area fullscreen passes — negligible). BANKED LEAD (player researching): angle-of-attack-dependent blur — the raw mirror and the blurred B both exist, so the water blend could mix them per fragment by incidence angle.
- **crates/mgc-render/src/lib.rs::blur_sky (sky bitmap pre-blur)** — the resolved 256x256 parallax sky is softened once at load by a separable 7-tap gaussian (sigma 1 texel, torus-wrapped both axes) before upload. The bitmap was authored for ~1:1 texel:pixel at 320x200; at modern resolutions one texel spans many screen pixels and bilinear upscaling exposes the texel grid and the period dither as soft blocks. Player-ruled 2026-08-08: "the sky, being the original parallax texture, is heavily pixelated… genuinely looks better blurred — a net win regardless of everything else." Same rationale class as the existing linear-filter choice (chunky texels at modern res read as noise, not as the authored look). Applies everywhere the texture is sampled: the sky pass, the mirrored sky, and the fog/extinction melt targets — all stay mutually consistent.
- **crates/mgc-app/src/config.rs::RenderEnhancement::smooth_motion** — defaults ON: entities render interpolated between the last two sim ticks (frame-smooth at any fps). Retail steps at tick rate. Presentation only; toggle off for the tick-stepped retail look.
- **crates/mgc-app/src/config.rs::RenderEnhancement::smooth_shading** — defaults ON (player ask): terrain shade interpolated across tile centers instead of retail's one shade level per tile. Preference-class (retail MC2 itself ships a Shift+F7 flat-shading toggle); T / the menu restores the faceted retail look.
- **crates/mgc-app/src/config.rs::RenderEnhancement::hud_transparency** — default OFF (opaque HUD) for readability, especially the radar; MC1 always draws the HUD translucent and MC2 adds the opaque toggle. Faithfulness is deliberately not scored for this preference.
- **crates/mgc-app/src/config.rs::RenderEnhancement::map_owned_buildings** — opt-in that brings MC2's dwelling map markers to MC1 (unclaimed = steady pink, claimed/possessed = flashing owner colour, 1px): retail MC1 never marks buildings. Default off.
- **crates/mgc-app/src/config.rs::RenderEnhancement::map_marker_scale** — opt-in slider (default 1.0×, range 0.5–3×): scales the overhead maps' entity dots and icon stamps together, on the map screen AND the radar. There is no faithful size to anchor to — retail plots surface-pixel markers, near-invisible at modern resolutions, and the port's tile-texel dots are already the (working) deviation baseline. Off 1.0× the dots leave the map-texture bake and draw screen-space at a constant size: radar zoom no longer grows them (compensated against the default 128-tile zoom; player ruling 2026-08-07 "zoom should compensate so they stay constant"). At exactly 1.0× the previous baked-texel rendering is byte-identical.
- **crates/mgc-app/src/config.rs::RenderEnhancement::map_marker_icons** — opt-in (default OFF): swaps chosen map dots for miniature crops of the entity's own WORLD sprite, drawn at HALF what the spell-stamp rule would give (min(native, 12)/2 per axis — player-sized 2026-08-07: the first build's shared 12-px cap read double the other map marks; scaled by map_marker_scale). Families (player-chosen 2026-08-07): spell jars/tokens (MC1/HW class 12 red AND blue, MC2 12/15); MC1 statues (2,1)/(2,3) and the dolmen regen shrine (2,2); MC2 marker stone (2,1) and dolmen (2,2). Retail draws dots only. Precedence: the expose-jar-spells DEBUG option outranks the swap for jars (spell icon + retail dot, never the jar miniature too). Icons capture LAZILY at a family's first sighting (`capture_marker_icons`, atlas re-uploaded on growth) — a load-time scan missed most MC2 tokens, whose authored THINGs are predominantly disposition-gated and spawn mid-play (player-reported as "MC2 jars stay red dots"); a family whose icon build fails keeps its dot, and the MC2 blink-phased stone/dolmen icons draw STEADY.
- **crates/mgc-app/src/config.rs::RenderEnhancement::map_extent_fog** — opt-in (default OFF): the map screens fade to black beyond the world's true extent — ±half a world period from the player, a rectangle axis-aligned in world space that therefore rotates with heading on the player-centered map. Purpose: the world is toroidal and the map repeats every entity past the seam; retail simply shows the duplicates. Soft 16-tile fade, drawn as the TOPMOST map layer (over terrain, dots, stamps, ants, objective marks; under the book/roster UI — player ruling 2026-08-07). The round radar geometrically never shows duplicates (zoom caps at 224 tiles < the 256 period), so it is untouched.
- **crates/mgc-app/src/config.rs::GameplayEnhancement::prune_owned_jars** — defaults ON: removes any spell jar whose spell the local player already owns (and can never pick up). Retail (both MC1 and MC2) leaves such jars in the world forever. Sweeps at level load and the instant the player gains a spell; disable with `--no-prune-owned-jars`. The MC1 mechanism behind "forever" is now ported and cited: a PHASE-2 jar RE-MINTS ITSELF on pickup (`sub_55D30` remc1 :64875-84 runs the spell's own ctor thunk at the jar's position after the grant converts the picked-up record into the owned token), so a phase-2 pickup point is permanent by construction rather than merely un-consumed — phase 1 (`sub_55DB0` :64904) has no such arm and IS consumed. With this option ON the re-minted jar self-culls on its next tick, so the default surface is unchanged; `--no-prune-owned-jars` now shows retail's actual behaviour.


## mgc-app — frontend/map/settings + mgc-render notes

- **entities.rs::map_dots_from_poses (MC1 dwelling map markers)** — PLAYER-RULED (2026-07-23): retail MC1 gameplay shows NO map markers for buildings, claimed or not, so the faithful default plots none. The decompile's sub_48710 LABEL_32 (:57271-78) nonetheless contains a house arm (unclaimed = LUT[3856] magenta, claimed = steady byte_99B58 even entry via id24) — the arm's apparent unreachability in practice is unexplained; observed original gameplay outranks the decompile. The `map_owned_buildings` enhancement (default OFF) draws MC2's law instead: unclaimed = steady 0xF0F magenta (the shared MC2 UNPOSSESSED code), possessed = the owner team pair blinking at MC2's flash cadence ((turn / 3) & 1 — the MC1 claimed-ball phase reads ~2.7x slower), all 1px.
- **entities.rs::mc2_map_dots / map_dots_from_poses (MC2 minimap)** — MC2's minimap COLOURS (DrawMinimapEntities_B_61A00, team pairs, blink phases) are drawn over an MC1-shaped rotating-radar projection; the projection itself stays MC1's, only the colours follow MC2. Deliberate approximation. (Also banked interim: class-3 castle/balloon, class-11 X-markers and class-10 flag families draw as 2x2 dots until the MSPRD stamp bank is baked; castle rope-line and Beyond-Sight enemy reveal wait for MC2 castles/rivals.)
- **worldmap.rs::WorldMap::click (travel sample timing)** — Retail gates the carpet travel sample on leg length and starts it late (MI:3786); the port plays it immediately with the click (still only when the flyer actually moves). Deliberate.
- **settings.rs::registry (render.enhancement.smooth_motion)** — Faithful value is OFF (retail steps everything at sim rate); the option ships default-ON as a deliberate default deviation (Preference-class, so it does not flag the run).
- **settings.rs::registry (gameplay.enhancement.prune_owned_jars)** — Faithful value is OFF (retail leaves owned spell jars in the world forever); ships default-ON as a deliberate default deviation (Preference-class cleanup, does not flag the run).
- **frontend_mc1.rs::Mc1Menu.player_name / EditName modal** — MC1 rename dialog pre-fills the current save name (edit rather than retype), and retail's two prompts ("Enter your name" + "Enter your call-name", etext 34/35, only the call-name used) are collapsed to ONE name field. Deliberate, matching MC2's name dialog.
- **frontend_mc1.rs::Mc1Menu::escape / key_enter (MC1 menu keys)** — PLAYER-RULED (2026-07-24): the MC2 retail menu key law (Esc = the Exit button when nothing is open, MI:5842-43; on any dialog Enter = OK / Esc = Cancel, the `DrawScrollDialog2_7B660` scancode 28/1 arms, MI:5656-63) is applied to the MC1 frontend as well, whose own retail key handling was never traced. A second Esc always declines what the first one opened. (The MC2 menu and world-map dialogs got the same law by straight fidelity — the earlier "Esc never quits" deviation here is RETIRED: Esc still never quits directly, it arms the confirm exactly as retail does.)


## mgc-render

- **ui.wgsl::vs_main (pixel snap)** — Quad corners are ROUNDED to the pixel grid. The UI is authored at a fixed resolution and scaled by an arbitrary factor, so edges land between pixels and adjacent sprites (a scroll's pieces, a panel behind a button) leave a hairline gap or overlap. Rounding the corners rather than the origin keeps neighbours welded: two quads sharing an edge round it identically. Retail scaled nothing, so there is no retail behaviour to be faithful to.
- **ui.wgsl::fs_main (half-texel clamp)** — Sampling is clamped to the sprite's own atlas cell. `sprites::pack` leaves NO gutter between packed sprites, so a fragment reaching the far edge samples the NEIGHBOURING sprite — one stray row or column of foreign pixels along the edge (player-reported, with screenshots). Half a texel in from each side keeps every sample inside the cell without shifting the interior.
- **anti-aliasing (`render.preference.anti_aliasing`, default OFF)** — Off / MSAA / supersample 1.5x / supersample 2x. No retail analogue — DOS drew one 320x200 buffer and filtered nothing — so it is a display knob like vsync, fidelity-free either way. Off is the exact previous path: no offscreen buffer, no resolve pass, single-sampled pipelines. The two techniques are NOT equivalent here: creature and building silhouettes come from discarding palette index 0 (hard 1-bit transparency), and a discard kills every sample of its fragment, so MSAA cannot soften them — it reaches true geometry only, chiefly terrain against sky. Supersampling re-evaluates the discard at higher resolution and is the only mode that smooths sprites. MSAA is baked into all nine pipelines at construction, hence startup-only; the supersample factor is live. Player-measured on integrated graphics: off 175 fps, 1.5x 130, 2x 95, 3x 50 — 3x was dropped as both too costly and visibly destructive to thin HUD marks.

- **crates/mgc-render/src/lib.rs::BOOK_MAP_ZOOM** — The MC1 book-screen map spans the FULL 256-tile world instead of retail's ~251-tile zoom (retail passes 382/378/a8=170, leaving its edges clipped — the "questionable things at the edges"). Deliberate: span the whole world so nothing is cut; toroidal wrap makes it appear infinite. The original's rounding-error void-mobs that live at that wrap are not reproduced.
- **crates/mgc-render/src/lib.rs::MC2_MAP_VIEW_SPAN_TILES / map_pane_zoom** — The MC2 map-screen pane is zoom-faithful (318.75 tiles vertically = retail `DrawMinimap_63600` scaling 204 units/px over the 400px pane, EF:21840-49), but retail blits the TERRAIN as a square 318.75-tile region squished into the 382-wide strip while its ENTITY layer runs isotropic at 204 units/px — a ~4.6% horizontal terrain-vs-entity misalignment baked into retail. Deliberately NOT reproduced: both our layers use the isotropic entity geometry (304.4 tiles across the native width), so map stamps sit exactly on their terrain. At non-4:3 windows the vertical span stays anchored at 318.75 and only the horizontal wrap widens.
- **crates/mgc-render/src/lib.rs::conceal_visibility + mgc-app entities.rs::billboards_from_poses (retail proximity concealment)** — The MC2 wraith (5,26) — and dwellers (5,23) under the `mc2_dweller_invisibility` patch — materialize only inside retail's OWN fog band, independent of the configured fog distance. Retail law: every sprite hard-culls at 20 tiles (`tileRenderCutOffDistance` = 5120², GRO:3498) with fog saturated from 19 (GRO:3505-11), and the hunting wraith's mode-2 ghost (flags bit 23, 33%-opaque, never fogged) reads as a faint smudge against that saturated backdrop — the player-remembered "zombies fade into existence up close". The port's extended fog (fog_distance default 50, faithful 20) had left the ghost crisply visible to ~5× retail's radius. Reconstruction wrinkle (deliberate): retail's literal math is an unfogged ⅓-blend to 20 tiles then a hard pop; we express the compound (backdrop fog saturation + cull + sub-pixel size at 320x200) as an alpha ramp on retail's fog-row law — full at ≤15 tiles, zero at ≥19, linear in d² between — because against our defogged terrain the literal pop-in would read far harsher than retail ever did. Other flag-ghost carriers (doomsday pyramid wait phase, kraken-tree emerge, fire-sphere walls, death fades) are deliberately NOT concealed — their long-range visibility under the extended fog is part of the certified fog/sky deviation (pyramid ghost re-verified 2026-08-04); revisit per-model only on a player sighting. METRIC RULING (player, 2026-08-08): the concealment volume is a SLANT-distance SPHERE for every concealed entity — one shape, no per-model special cases. Retail's literal fog/cull metric is HORIZONTAL in both games — verified at all four consumers (MC2 sprites GRO:3498 / terrain GRO:1040, MC1 sprites remc1:36843 / terrain remc1:34456, identical constants) — but the shape was unobservable in retail play (both games cap flight far below the 15-tile band, and the shrinking clear disc under a climbing carpet reads as a dome, which is how the player remembers retail: omnidirectional). The sphere also matches the port's own fog metric (3D fragment distance), which is the very thing this law stands in for; a climbing balloon melts away like a receding one.
- **crates/mgc-render/src/lib.rs::render (mirror pass, mirrored sprites)** — The water-reflection pass mirrors OPAQUE billboards (world sprites) into the water. Retail (remc2 GameRenderOriginal reflects terrain only) never reflects sprites. Deliberate presentation choice: a monster over water should show in the water. Translucent sprites are excluded (not worth a sorted blend pass).
- **crates/mgc-render/src/map.wgsl::fs_fog + lib.rs::project_map_dots** — the render half of the 2026-08-07 big-map options (see the config entries for `map_marker_scale` / `map_extent_fog`): the extent-fog overlay reuses the map pane's own quad + globals with a second fragment entry, and the screen-space dot projector reuses the stamp projector's wrap-image walk. At `marker_scale == 1.0` neither path runs — the dots stay baked texels, bit-for-bit the prior rendering.
- **crates/mgc-render/src/lib.rs::zoom_map_screen (+ the `+`/`-` dispatch in mgc-app lib.rs)** — MAP-SCREEN zoom (player ask 2026-08-07): `+`/`-` while the map screen is open zoom IT instead of falling through to the radar (each surface owns its zoom). A port addition with no retail analogue (retail's book map has no zoom; the radar `+`/`-` is also ours). Multiplier on the layout's base span, clamped [1/8, √2]: from a 32-tile crop on the MC1 book out to the span that keeps the WHOLE rotated world square on the pane at any heading (the extent's diagonal is base·√2 — at 1x a 45° heading clipped the corner; applies to the MC2 split pane too). Session-only, never persisted. Screen-space markers size against the UNZOOMED base, so zoom never resizes them — the same compensation rule as radar zoom. At the √2 stop a wide pane's far edges can show wrap regions beyond ±1 period whose stamp/dot images the projectors don't emit — PLAYER-RULED fine: those regions are >256 tiles out, deep inside the extent fog's full black, and unfogged they are duplicate territory anyway.


## mgc-app — movie.rs (FMV player)

- **RATE_SCALE (playback 25% slower than authored)** — PLAYER-RULED. The scripts cue a narration clip and then load the next sample bank a fixed number of frames later, and a bank load stops every voice, so a clip that outlasts its scene is cut mid-sentence. At the authored rate MC1's intro clips the last book page's line by about a second — a couple of words. Retail does the same (the timing is simply tight); stretching every delay by a quarter gives the voice room and leaves the relative pacing untouched. The factor is MEASURED against the real clip lengths (`narration_clips_fit_before_their_bank_is_swapped`): the binding constraint is `voc11`, 6.13 s of speech in a 5.11 s scene, which needs 1.20; 1.25 is that plus headroom.
- **SCRIPT_LEAD (script fires one frame early)** — PLAYER-RULED. Retail's order is events(N) → draw(N) → wait, which the port implements; played back, the long scene holds still landed a frame or two into the NEXT page-flip instead of on the settled page. Leading by one frame parks the hold where the animation rests. The transcription itself is unchanged.
- **MoviePlayer::next_stream / ::skip (a movie's score ends with it)** — PLAYER-RULED. Retail carries the music across movie boundaries: `M`/`Z` start a track and nothing stops it, so MC1's intro theme plays on under the flaming title, which has no music cue of its own to replace it. Reported on both paths (the skip first, then the natural transition, which the skip fix had masked). The track belongs to the movie and now ends with it, however it ended. The sample half of the same teardown IS faithful — retail's per-movie bank reload stops every voice.
- **script::SCRIPTS (`levelw2` gets its own table)** — PATCH OPTION `gameplay.patches.win2_movie_score` (default patched; was always-on until 2026-08-08). Retail points BOTH win movies at one event script (`dword_4A5D8_4A918`), so `levelw2` plays scored with bank 6's `win1` at frame 200. An unreferenced table at 0x4A5FC is byte-identical but for bank 7 (`win2`) and cue frame 180 — plainly `levelw2`'s own script, orphaned by the shared pointer, and bank 7 exists on the CD holding exactly `win2`. The port gives each movie its own table: a deliberate bug fix, not a transcription.
- **MoviePlayer::frame (centred letterbox)** — Retail ran one 320x200 mode and never letterboxed. Non-4:3 windows centre the picture with black bars; retail has no case to be faithful to.
- **MoviePlayer::tick (wall-clock pacing)** — Retail busy-waits on the shared 120 Hz counter and zeroes it per frame. We accumulate delta time at the same rate and DROP frames under stall rather than catching up, so a slow host loses frames instead of seconds.
- **script (per-movie delay reset)** — Retail's `dword_9ADC4` is a process global that persists across movies; we reset to the default per movie. Every transcribed table opens with an `'A'` record, so the two are equivalent — this just cannot drift.


## mgc-audio

- **lib.rs::Audio::play_movie_sample (movie cues bypass the mixer)** — FMV sample cues go straight to output voices instead of through `FaithfulMixer`. The mixer is a ported 3-D ruleset with per-id request slots and a listener; a movie has no world and no listener, and retail plays these onto voices directly too. No session is alive during a movie, so the channels are free.
- **movie.rs script (`'H'`/`'O'`/`'P'` volume operands flattened)** — MC2 starts two intro ambiences looping at volume 0 and raises them to 127/80 with a paired key; the port starts them at full, so they arrive without their fade-in and one is louder than retail.
- **mixer.rs::FaithfulMixer::tick (MC2 pitch jitter)** — Deliberate approximation: the emitter-action +10..+30 pitch variant of the gloop (sound id 46) is unmodeled; only the base ±10% (id 46) / ±15% (devil calls 42-44) per-play jitter is applied (Sound.cpp:6331-45). The jitter runs on the mixer's own LCG and audio is outside the sim hash, so this never affects determinism.
- **output.rs::lerp_i16 (resampling)** — Enhancement over retail: music/speech/SFX are resampled to the device rate by linear interpolation rather than nearest-frame (zero-order hold). Retail's nearest-neighbor read stair-steps the waveform into a "grainy, low-bit-depth" buzz; interpolation removes it.
- **output.rs::Channel::release (SFX declick)** — Enhancement over retail's hard cut: a `Stop` ramps the voice out over ~2.5 ms instead of clearing `pcm` in one sample. A hard mid-waveform cut is an audible click, and the meteor fire trail restarts the same voice ~24×/s so the clicks stack into a crackle. The ~2.5 ms fade removes them without altering the (faithful) retrigger cadence.
- **output.rs::MusicState::release (music declick)** — Same enhancement class as the SFX declick, applied to StopMusic and track replacement: ramp out over ~2.5 ms rather than a hard `pcm = None` cut (which is an audible click/thump).
- **output.rs::Renderer::render (suspend edge ease)** — Enhancement over retail's instant mute: on game pause the whole output eases toward mute over ~2.5 ms (`suspend_gain`) and back on resume, rather than stepping mid-waveform. Playback positions drift a few ms during the ramp, then hold.
- **lib.rs::Audio::tick (voiceover duck recovery)** — Deliberate approximation of retail's 120 Hz FadeUpSoundVolume: after a voice line ends, music+sfx ramp from 1/3 back to full over ~0.7 s of sim ticks. The exact per-callback step is treated as a volume-scale detail; the span is calibrated to be tick-rate-independent.


## mgc-import

- **adlib.rs::render (OPL3 song renderer)** — Note velocity is ignored entirely. The original HMI AdLib driver's velocity handling is unknown, and retail songs carry velocities 1..9 on busy melodic channels in every arrangement, which only renders sensibly if the driver used raw patch levels; ignoring velocity reproduces that. CC7 volume is honored only at note-on, not re-applied on sounding notes as live ramps (retail songs only set CC7 up front). Fidelity pass still owed — playtest is the oracle.

- **cdtracks.rs (module) — frames→ms conversion** — The port applies retail's frames→ms conversion (`× 13.33333333333` truncated) uniformly to ALL rows, including the secret-level rows 25/26. Retail's secret-level path skips the conversion, a latent bug that would cut those clips 13× short (trace §1b); the port fixes it rather than reproducing the bug.

- **xmi.rs::parse_evnt — AIL FOR-loops (cc116/117)** — MC2 wraps every sub-song in one infinite whole-song FOR-loop; the parser drops the cc116/117 pair and instead loops the baked FLAC. A cc116 FOR-loop that starts mid-song (a real interior section repeat) is FLATTENED — played through once rather than repeated. Two bank-0 songs contain one (C2GAME3 tick 1, C2INTRO tick 11876). The flattened render is emitted with an audible note.

- **The stuck explosion (mc1l32) — retail wedge NOT reproduced** *(deliberate, player-ruled 2026-08-08)*. Retail l32 wedges a (10,0) explosion at slot 2 from t=39508 to take end: flags 0x30086, life frozen at 6/8, floating mid-air — retail's dispatcher never advances it (mechanism undug; the player attributes it to the level's entity exhaustion, and the level is authored beyond the pool's means). The port kills it within one tick, and that is the KEPT behavior: "this is a bug that we should not reproduce; it serves no purpose." Evidence: recordings/mc1l32.mgcr, suite exemplars t=39509/39511 (expected-fail by design), roster rule `mc1l32-stuck-explosion-wedge`.
