# Claude local settings
.claude

# Build output
/target

# Original game data (bring your own GOG copy) — never committed
/gamedata/*
!/gamedata/README.md

# Baked assets and oracle fixtures — derived from game data, regenerated locally
/baked
/fixtures
/tools/mc2-genlevel/mc2-genlevel
/saves

# Reference decompilation checkouts (remc1, remc2) — local clones, never committed
/reference

# Per-user authenticity-matrix config: personal overrides + the
# app-generated faithful baseline
/mgcarpet.json

# Flight recordings
/recordings/*.mgcr

# Python caches
__pycache__
